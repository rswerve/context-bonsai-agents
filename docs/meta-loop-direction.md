# Context Bonsai Meta-Loop: Direction

This document states the end goal for the Context Bonsai meta-loop, the next step toward it, and the provisional steps after that. It is itself an iteration artifact: the process that produced it — examine the loop's current state, absorb what recent cycles taught, restate direction — is expected to run again, and a later iteration may overwrite the "Next Step" and "Provisional Future Steps" sections wholesale. Only the End Goal section should survive iterations largely intact; treat edits to it as scope changes requiring the project owner's approval.

## End Goal

A self-updating Context Bonsai that works for every supported harness.

Concretely, the system maintains three levels of artifact, each able to regenerate the one below it:

1. **Behavior contract** (`docs/context-bonsai-agent-spec.md`) — what Context Bonsai does, harness-independent. Changes only when the product changes.
2. **Per-harness update loop** — the routine procedure that carries a port onto each new harness release. Two upstream *shapes* exist today: for *git-fork* harnesses, rebase the patch chain and revalidate; for *closed-artifact* harnesses (Claude Code), freeze the new npm artifact, re-verify semantic anchors, and revalidate. This level encodes structural assumptions about the harness (package layout, seam locations, validation commands) that hold across the majority of releases.
3. **Per-release cycle** — one execution of level 2 against one new release, ending in the e2e release gate defined by `docs/agent-specs/context-bonsai-e2e-spec.md`.

The meta-loop is the machinery that generates and regenerates level 2. It must handle three entry conditions with one pipeline, differing only in how much prior work is still trustworthy:

- **Routine release** — level 2's assumptions hold. Generate and execute a cycle plan. This is the cheap path and the common case.
- **Structural break** — a harness release invalidates level 2's assumptions. The signals already exist as fail-closed behaviors: mass classification of commits into the hard-blocking `manual_review` bucket (the cycle generator's fallback for indeterminate evidence), anchor discovery scoring below the `minScore`/`minMargin` thresholds everywhere (the behavior spec's fail-closed anchor-ambiguity rule), or a Protocol A failure (the e2e spec's secret-prune oracle) on a clean build. The failure is the escalation signal: re-derive the level-2 assumptions from the behavior contract plus fresh exploration of the new harness architecture, demoting prior bindings to untrusted priors that must be re-verified, then re-enter the routine path.
- **New harness** — no prior work exists. Run the same derivation from nothing: capability discovery, integration-posture selection (per the behavior spec's change-minimization rule), per-harness spec, implementation, e2e binding, and emission of that harness's level-2 update loop as a first-class artifact.

"Self-updating" means the routine path runs with no human decision-making, and the other two paths run agentically with human review at the existing reviewer+judge gates. Detection of which path applies is itself automatic — a path-selection dispatcher, which does not exist yet: the routine path fails closed with a structural reason code rather than grinding forward or silently succeeding.

**Executor tiering.** The determinism machinery (frozen inputs, fixed precedence, machine-checkable gates) is calibrated scaffolding, not architecture: each such rule moves a judgment call out of the executor and into the artifact, and that is precisely what lets weaker or cheaper models run the loops. The assignment is per-harness, not global — three owner decisions set the current shape (the branch-1 ratification 2026-07-03, which up-tiered plan generation to the owner class while leaving execution on the weak tier; the Claude Code calibration-target direction 2026-07-03; the executor-model change 2026-07-04): **cycle-plan generation and validation always run on the owner (Fable-class) tier; execution of the approved plan and the routine's own §1.16 maintenance (section numbers throughout cite `docs/agent-specs/forward-port-spec.md`; §1.16 absorbs cycle friction back into the routine's instructions) run on the cheapest tier that has proven reliable for that harness** — for OpenCode, a process orchestrator (`.llm-conductor/ORCHESTRATOR_AGENT.md` pattern) on `openai/gpt-5.3-codex-spark`; for Claude Code, an Opus 4.8 subagent at low effort. (The OpenCode pilot's first four runs executed on GPT-5.5 via OpenCode's own Zen `opencode/*` subscription-hosted provider; a quota mis-route exposed that wiring, the owner forbade Zen for pilot runs and moved execution to his OpenAI subscription, which offers no GPT-5.5 — hence `gpt-5.3-codex-spark` from run 5b onward.) Two layers of scaffolding are now distinguished by evidence: the deterministic gates catch false *outcomes* (nothing false has ever sealed or pushed), and the §1.18 run-continuity mechanisms (intent log, reconciliation, report-before-stop) keep a weak executor's *account of its own actions* truthful across process boundaries — a failure layer the gates cannot reach, observed as confident confabulation on `gpt-5.3-codex-spark` before §1.18 existed. Executors below the owner tier get both layers; relaxing either may be explored only in tiers executed by the owner-class model. The loop above all of this — developing the loop system, governed by this document's Iteration Rule — runs on the owner-class model with the project owner.

## Current State (evidence)

Short SHAs are parent-repo commits unless stated; side-repo commits are marked. Every recorded stumble carries one of two verdicts: **SPEC-GAP** (the artifact left a judgment call unresolved — fix the artifact) or **EXECUTOR-FAIL** (the artifact was deterministic and the executor still failed — evidence about the tier). Full pilot narrative: `.agents/pilot/gpt55-v1.17.13-observer-log.md` (runs 1–4 committed at `20d5d40`, `f9d9063`, `bddcf96`, `cb0a5fb`; runs 5–6 records under `.agents/pilot/archive/`).

- **The routine path is specified in one layered artifact and has now survived live cycles on both shapes.** `docs/agent-specs/forward-port-spec.md`: shape-agnostic core, git-fork and closed-artifact shape bindings, per-harness slots (OpenCode and Claude Code bound; cline, codex, gemini-cli, kilo, pi unbound). Four spec extensions landed from live-cycle evidence in 2026-07-05's runs: §1.15's **target-resolution rehearsal** (`461dc8b`, git-fork shape) — rehearse the full replay sequence plus Phase-4 validation in a scratch worktree at the target upstream before approval, because ported commits can apply conflict-free yet import source-era APIs absent from the target; §1.15's **transform-application rehearsal** (`f89be0b`, closed-artifact shape) — the analogue for the other shape: in a scratch clone at generation time, realize the plan-bound `updated_anchor` edits and run the slot's test/typecheck/artifact-evidence commands against the frozen target bundle, catching before approval the drift class (transforms passing the port's tests yet failing on the target) that Claude Code calibration runs previously caught only at execution; **§1.17 as a 20-code escalation registry** (`5e80849`, below); and **§1.18 run continuity for weak executors** (`031e533`) — intent log with command-sourced timestamps, clean-start authorship invariant, evidence-based reconciliation, report-before-stop with evidence-verified closure, relaunch-as-resume.
- **Closed-artifact shape: the Claude Code forward-port cycle is executed and sealed.** The side repo's port moved from `@anthropic-ai/claude-code@2.1.156` to 2.1.200 (2026-07-03): plan generated and §1.15-validated on the owner tier, then calibrated on Opus-4.8-low over three scratch-clone runs (run 1: 2 SPEC-GAP + 2 EXECUTOR-FAIL; Amendment 1 reviewed and committed; run 2 fully clean; Amendment 2 bound away the two recurring slips; run 3 clean and slip-free) before the real-cycle execution ran the same artifact against the real repo unchanged, first try, clean — sealed (every machine-checkable seal gate passing) with one in-run-recovered SPEC-GAP (E2E-08 boundary-marker echo, carried in the committed §1.16 maintenance report). Owner-authorized push landed the side repo (`origin/main` = side-repo `4bd8747`) and the submodule pin (`7c44a04`). Residual: the Phase-8 staged-artifact re-home awaits the owner's Landing Authorization (his explicit go-ahead, recorded in the plan's approval section). This is the first full instance of the branch-1 tiering executing end-to-end on a second harness: owner-tier generation, weak-tier execution, calibration-run iteration in between.
- **Git-fork shape: the OpenCode v1.17.13 cycle is complete up to publish, on the changed executor model.** After run 5's false start exposed a provider mis-route (the pilot had been on OpenCode's Zen subscription; owner moved execution to `openai/gpt-5.3-codex-spark`, 2026-07-04), runs 5c/5d failed identically at harness auto-continue boundaries: the model lost authorship of its own in-run actions and confabulated coherent false accounts (its own green baseline reported as an upstream regression; its own worktree reported as a pre-existing collision; two final reports contradicting repository state). Six EXECUTOR-FAIL verdicts, zero SPEC-GAP — the plan bindings GPT-5.5 had navigated cleanly in runs 2–4. The owner's response was to engineer around the amnesia rather than up-tier: intent-log + reconciliation scaffolding (`500e8a9`), deliberately changing what the pilot measures — the executor *with* continuity scaffolding.
- **Run 5e validated that scaffolding and surfaced the cycle's one real SPEC-GAP.** Across eight process lifetimes and seven resumes (auto-continue exhaustion every ~25–30 min; two provider-quota sleeps), every report stayed truthful, one false prose claim was disproven by the next resume's reconciliation, and the run ended in an observer-verified genuine fail-closed STOP at Phase 4: the replayed commits import v1.15.7 APIs (`@/bus/bus-event` et al.) absent in v1.17.13 — a drift class invisible to classification and conflict-free cherry-picks, which §1.15 had not tested for. Final ledger: one EXECUTOR-FAIL (false closing claims, corrected by the run's own closeout resume), a four-item tuning batch applied to the brief (`d24540f`), and the SPEC-GAP that produced the §1.15 rehearsal.
- **The owner tier closed that gap and run 6 executed the result clean.** The port was re-implemented against v1.17.13 (the cherry-picks had pasted the v1.15.7 monolith over upstream's schema/core refactor; the real capability re-landed minimally and rehearsed green), and plan revision 2 (`8d8af58`) re-bound Phase 3 to `git am` of a checksummed patch artifact with §2.5 re-implement rows. Run 6 (2026-07-05, 16 minutes, one process, zero resumes): Phases 0–6 clean on first attempt, all seal gates green, and a correct fail-closed refusal to seal at gate 11 (`credentials-missing-in-harness` — the `OPENCODE_*` launch credentials have been unset since run 4). **Zero EXECUTOR-FAIL, zero SPEC-GAP; the first run where every report claim survived independent verification unamended.** Live cycle state, preserved: replay worktree with 3 commits, baseline commit `5054f94`, local-only tag `bonsai/v1-on-opencode-1.17.13`; nothing pushed.
- **What the two cycles jointly establish about tiering**: a deterministic, rehearsed artifact executes cleanly first-try on weak executors of two different tiers and harnesses — but the tier below Opus-4.8-low needed the §1.18 continuity layer to get there, and rehearsal (executing the exact replay/patch sequence during generation) is what converted run 5e's Phase-4 STOP into run 6's 16-minute clean pass. Generation remains owner-tier work on both harnesses.
- **Escalation is formalized; the dispatcher that consumes it is not built.** §1.17 (`5e80849`) is now a 20-code registry mined from the spec's and behavior contract's existing STOP/hard-fail points: every run/cycle-ending STOP records exactly one code with a machine-checkable emission condition (source-section cited), an invalidation scope in the spec's layer terms, and a resumption route. The routing rule delivers the required containment: only `structural-*` codes enter level-2 re-derivation; environment, invoker-input, generation, and executor classes resume within the routine path. The two previously informal structural triggers ("a mass" / "most of the registry") are bound to strict >50% majorities as fixed defaults; the §1.18 confabulation class is explicitly a non-code path (observer-attributed EXECUTOR-FAIL per §1.16). All five live-corpus STOP classes — launch-environment (run 1), §1.15 non-convergence (runs 2–4), executor-integrity (5c/5d), target-API drift (5e), environmental-input gaps (run 6's credentials STOP) — map to codes or the stated non-code path. What does not exist is the path-selection dispatcher itself: the automation that reads an emitted code and routes to routine-resume vs. re-derivation without a human doing the reading.
- The new-harness derivation path is unchanged: one bespoke epic (`.agents/plans/epic-context-bonsai-agent-ports/`), no reusable onboarding procedure extracted.

## Next Step

**Specify the derivation pipeline** (the structural-break / new-harness path): capability discovery against the behavior contract, integration-posture selection, per-harness spec generation, e2e slot binding, and emission of the harness's level-2 bindings. Mine `.agents/plans/epic-context-bonsai-agent-ports/` and the per-harness specs' shared section structure for the stage contracts. Its entry conditions now exist as a contract: the §1.17 `structural-*` codes, each carrying the invalidation scope that tells the pipeline which prior bindings it must demote to untrusted priors.

Acceptance test: the pipeline is specified in the spec's own layer terms with staged, reviewable outputs; a clean-room reader can trace how each of the two historical derivations (the OpenCode port epic; the Claude Code closed-artifact port) would have flowed through its stages; and the structural-break entry is bound to the §1.17 codes with no gap (every `structural-*` code's invalidation scope maps to a pipeline entry stage).

Two owner-gated items remain open in parallel and complete whenever the owner acts; neither blocks the step above:

- **Finish the OpenCode v1.17.13 cycle**: the owner supplies `OPENCODE_PROVIDER`/`OPENCODE_MODEL`/`OPENCODE_API_KEY` to the launch environment (requested 2026-07-05, delivery-verified; still absent as of this iteration), then the blocked e2e scenarios re-run and — separately owner-gated regardless of gate outcomes — the publish ladder executes. The run-6 state above is the live cycle state; the next run resumes at the credentials gate under §1.18 relaunch-as-resume, not from scratch.
- **Finish the Claude Code 2.1.200 landing**: obtain the owner's Landing Authorization, then execute the remaining Phase-8 scope (staged-artifact re-home from the side repo into the parent, stale-target-evidence spec lines, final verification, pin advance).

## Provisional Future Steps

Ordered by current best guess; any or all may be replaced when the next iteration of this direction loop runs.

1. **Build the path-selection dispatcher.** §1.17 supplies its input contract; the derivation pipeline (Next Step) supplies its expensive branch. The dispatcher is the automation that reads an emitted reason code and routes — routine-resume by the code's recorded route, or level-2 re-derivation for `structural-*` — without a human interpreting the STOP.
2. **Split per-harness specs into contract vs. bindings.** The stable behavioral content and the regenerable structural facts currently share one document; the derivation pipeline needs to know what it may rewrite.
3. **Reduce human steps in the routine path** — automated release detection per harness, automatic cycle-plan generation on detection. Its prerequisites are now met: the spec has survived live cycles on both shapes, and §1.17 plus the §1.15 rehearsals make unattended STOPs legible and rarer. What this step still waits on is the dispatcher (step 1), so an unattended STOP routes itself.

## Iteration Rule For This Document

When this direction loop runs again: re-read this document, compare its Current State section against the repository, rewrite Next Step and Provisional Future Steps freely, and preserve End Goal unless the project owner approves a scope change. Supersede in place (this file, same path) so there is exactly one current direction statement.
