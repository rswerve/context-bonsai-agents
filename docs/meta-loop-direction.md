# Context Bonsai Meta-Loop: Direction

This document states the end goal for the Context Bonsai meta-loop, the next step toward it, and the provisional steps after that. It is itself an iteration artifact: the process that produced it — examine the loop's current state, absorb what recent cycles taught, restate direction — is expected to run again, and a later iteration may overwrite the "Next Step" and "Provisional Future Steps" sections wholesale. Only the End Goal section should survive iterations largely intact; treat edits to it as scope changes requiring the project owner's approval.

## End Goal

A self-updating Context Bonsai that works for every supported harness.

Concretely, the system maintains three levels of artifact, each able to regenerate the one below it:

1. **Behavior contract** (`docs/context-bonsai-agent-spec.md`) — what Context Bonsai does, harness-independent. Changes only when the product changes.
2. **Per-harness update loop** — the routine procedure that carries a port onto each new harness release. Two upstream *shapes* exist today: for *git-fork* harnesses, rebase the patch chain and revalidate; for *closed-artifact* harnesses (Claude Code), freeze the new npm artifact, re-verify semantic anchors, and revalidate. This level encodes structural assumptions about the harness (package layout, seam locations, validation commands) that hold across the majority of releases.
3. **Per-release cycle** — one execution of level 2 against one new release, ending in the e2e release gate defined by `docs/agent-specs/context-bonsai-e2e-spec.md`.

The meta-loop is the machinery that generates and regenerates level 2. It must handle three entry conditions with one pipeline, differing only in how much prior work is still trustworthy:

- **Routine release** — level 2's assumptions hold. Generate and execute a cycle plan. This is the cheap path and the common case.
- **Structural break** — a harness release invalidates level 2's assumptions. The signals already exist as fail-closed behaviors: mass classification of commits into the hard-blocking `manual_review` bucket (the cycle generator's fallback for indeterminate evidence), anchor discovery scoring below the `minScore`/`minMargin` thresholds everywhere (the behavior spec's fail-closed anchor-ambiguity rule), or a Protocol A failure (the e2e spec's secret-prune oracle) on a clean build. The failure is the escalation signal: re-derive the level-2 assumptions from the behavior contract plus fresh exploration of the new harness architecture, demoting prior bindings to untrusted priors that must be re-verified, then re-enter the routine path.
- **New harness** — no prior work exists. Run the same derivation from nothing: capability discovery, integration-posture selection (per the behavior spec's change-minimization rule), per-harness spec, implementation, e2e binding, and emission of that harness's level-2 update loop as a first-class artifact.

"Self-updating" means the routine path runs with no human decision-making, and the other two paths run agentically with human review at the existing reviewer+judge gates. Detection of which path applies is itself automatic — the routine path fails closed with a structural reason code rather than grinding forward or silently succeeding, and a path-selection dispatcher routes the code without a human interpreting the STOP.

**Executor tiering.** The determinism machinery (frozen inputs, fixed precedence, machine-checkable gates) is calibrated scaffolding, not architecture: each such rule moves a judgment call out of the executor and into the artifact, and that is precisely what lets weaker or cheaper models run the loops. The assignment is per-harness, not global — three owner decisions set the current shape (the branch-1 ratification 2026-07-03, which up-tiered plan generation to the owner class while leaving execution on the weak tier; the Claude Code calibration-target direction 2026-07-03; the executor-model change 2026-07-04): **cycle-plan generation and validation always run on the owner (Fable-class) tier; execution of the approved plan and the routine's own §1.16 maintenance (section numbers throughout cite `docs/agent-specs/forward-port-spec.md` unless a sentence names another spec, whose own numbering then applies; §1.16 absorbs cycle friction back into the routine's instructions) run on the cheapest tier that has proven reliable for that harness** — for OpenCode, a process orchestrator (`.llm-conductor/ORCHESTRATOR_AGENT.md` pattern) on `openai/gpt-5.3-codex-spark`; for Claude Code, an Opus 4.8 subagent at low effort. (The OpenCode pilot's first four runs executed on GPT-5.5 via OpenCode's own Zen `opencode/*` subscription-hosted provider; a quota mis-route exposed that wiring, the owner forbade Zen for pilot runs and moved execution to his OpenAI subscription, which offers no GPT-5.5 — hence `gpt-5.3-codex-spark` from run 5b onward.) Two layers of scaffolding are now distinguished by evidence: the deterministic gates catch false *outcomes* (nothing false has ever sealed or pushed), and the §1.18 run-continuity mechanisms (intent log, reconciliation, report-before-stop) keep a weak executor's *account of its own actions* truthful across process boundaries — a failure layer the gates cannot reach, observed as confident confabulation on `gpt-5.3-codex-spark` before §1.18 existed. Executors below the owner tier get both layers; relaxing either may be explored only in tiers executed by the owner-class model. The loop above all of this — developing the loop system, governed by this document's Iteration Rule — runs on the owner-class model with the project owner.

## Current State (evidence)

Short SHAs are parent-repo commits unless stated; side-repo commits (each harness port lives in its own repo, checked out here as a submodule — "the side repo" in a Claude Code context is `tweakcc_context_bonsai`) are marked. Every recorded stumble carries one of two verdicts: **SPEC-GAP** (the artifact left a judgment call unresolved — fix the artifact) or **EXECUTOR-FAIL** (the artifact was deterministic and the executor still failed — evidence about the tier). Full pilot narrative: `.agents/pilot/gpt55-v1.17.13-observer-log.md` (runs 1–4 committed at `20d5d40`, `f9d9063`, `bddcf96`, `cb0a5fb`; runs 5–6 records under `.agents/pilot/archive/`).

- **The routine path is specified in one layered artifact and has survived live cycles on both shapes and the pure-extension variant** (pi's posture, bound as a Part 3 variant at `e74a737`: an open published package consumed via public API with nothing patched, so "patch application" slots state their realization as the e2e harness's pinned fresh install). `docs/agent-specs/forward-port-spec.md`: shape-agnostic core, git-fork and closed-artifact shape bindings, per-harness slots — **six of the seven harnesses are now bound in Part 4** (OpenCode §4.2, Claude Code §4.3, pi §4.4, codex §4.5, cline §4.6, kilo §4.7; §4.8 Unbound holds only gemini-cli). Spec extensions landed from live-cycle evidence: §1.15's **target-resolution rehearsal** (`461dc8b`, git-fork shape) and **transform-application rehearsal** (`f89be0b`, closed-artifact shape) — rehearse the replay/transform sequence against the frozen target at generation time, catching before approval the drift classes previously caught only at execution; **§1.17 as a 20-code escalation registry** (`5e80849`); **§1.18 run continuity for weak executors** (`031e533`); **§1.19 scratch-state lifecycle** (`603bf5e`, owner directive 2026-07-06: every procedure deletes its own /tmp and scratch state once it has served its purpose — plans carry explicit removal rows on seal and STOP paths, and §1.15 reviewers block on their absence); **§1.20 weekly update cadence** (`9c9ff68`, owner directive 2026-07-06: a 7-day per-harness rate limit on routine cycle *starts* — detection stays continuous — with a committed last-cycle-start ledger `docs/agent-specs/cycle-cadence-ledger.json`, an owner-declared major-announcement override, and a machine-checkable gate `scripts/check-cycle-cadence.mjs` that parses the spec at runtime and fails closed, 15 tests); and §1.16 maintenance folds from every sealed cycle that produced findings (`2d541ce`, `2b879bb` for Claude Code; `30ef528` for cline; `725a7ae` for codex — the fold path is routine).
- **Closed-artifact shape: two consecutive Claude Code cycles are sealed and landed, the second materially cheaper.** The 2.1.156→2.1.200 cycle (2026-07-03; landed `c5b8271`) took three calibration runs plus two plan amendments before the real cycle ran clean. The 2.1.200→2.1.201 cycle (2026-07-05; landed `e76f082`, side-repo pin `130afeb`) ran the now-extended routine end-to-end in one day: the detector surfaced the release, the generation-time §1.15 transform-application rehearsal ran green (zero anchor drift, offsets uniformly +23 bytes), calibration runs 1–3 were all clean on the unchanged plan (zero STOPs, ~19–25 min each; recurring slips were EXECUTOR-FAIL, recovered in-run), and the real cycle sealed in ~24 minutes with gates 1–13 passing. Mid-run, concurrent landing commits advanced the side repo's `main`; the executor's §1.18 reconciliation absorbed the tip advance and re-verified byte-identity and change-scope at final HEAD — the first live validation of §1.18 on the Opus tier. Phase 8 landed without an owner gate under the standing order abolishing per-landing authorizations for local-only work (owner directive, 2026-07-05): local landings are routine; only outward actions (push, publish) remain owner-gated.
- **Git-fork shape: the OpenCode v1.17.13 cycle is complete up to publish, on the changed executor model.** Runs 5c/5d failed at harness auto-continue boundaries with confident confabulation (six EXECUTOR-FAIL, zero SPEC-GAP); the owner engineered around the amnesia with intent-log + reconciliation scaffolding (`500e8a9`) rather than up-tiering. Run 5e validated that scaffolding across eight process lifetimes and surfaced the cycle's one real SPEC-GAP (replayed commits importing v1.15.7 APIs absent in v1.17.13 — the gap that produced the §1.15 rehearsals). The owner tier re-implemented the port against v1.17.13 and re-bound the plan (revision 2, `8d8af58`); run 6 executed it clean in 16 minutes — zero EXECUTOR-FAIL, zero SPEC-GAP, correct fail-closed no-seal at gate 11 (`credentials-missing-in-harness`). Live cycle state, preserved: replay worktree with 3 commits, baseline `5054f94`, local-only tag `bonsai/v1-on-opencode-1.17.13`; nothing pushed. The credentials-gate resume is owner-blocked (see Next Step's parallel item).
- **What the cycles jointly establish about tiering**: a deterministic, rehearsed artifact executes cleanly first-try on weak executors across five harnesses and two tiers — but the tier below Opus-4.8-low needed the §1.18 continuity layer to get there, and generation-time rehearsal is what converts execution-time STOPs into clean passes. Generation remains owner-tier work on all harnesses.
- **The derivation pipeline is specified, and its tail is proven by repeated execution — three consecutive harnesses.** `docs/agent-specs/derivation-pipeline-spec.md` (`6a09f7c`, pi-lesson fold `b49d69a`): six staged, reviewer+judge-gated stages — capability discovery, integration-posture selection, per-harness spec generation, implementation, e2e slot binding, level-2 binding emission — mined from the port epic's failure record and the per-harness specs' shared skeleton. Entry conditions are a closed contract: all five §1.17 `structural-*` codes map to an entry stage plus an untrusted-prior demotion set (the pipeline spec's own §2.2 table). **Stages 5–6 have now executed live four times**: pi (2026-07-05, `abc3465`/`e74a737`, acceptance plan `026eb37` against the real pending 0.73.1); codex (2026-07-06, `603bf5e` — §4.5 slot table, `docs/codex-e2e-runbook.md`, acceptance pressure-test plan `3113c6a`); cline (2026-07-06, `203bbb4` — §4.6 slot table, `docs/cline-e2e-runbook.md`, acceptance plan `d2f00bb`; the detector's `git-remote-tag` directive gained an optional literal tag suffix for cline's `v<version>-cli` family, narrowing-only and fail-closed, 16 detector tests); and kilo (2026-07-06, Stage 5 `1b180c5` — first live validation with a zero-credential local-ollama rig, Protocol A and E2E-01/02/03/06/07 PASS; Stage 6 `5f4df5b` — §4.7 slot table, detection seeded `bonsai/v1-on-kilo-7.2.20`). **Kilo's acceptance gate is the strongest yet**: the cycle plan was generated against the *real pending release* v7.4.1 in §1.11's `uncommitted-pressure-test` validation mode, and the §1.15 target-resolution rehearsal caught real upstream drift (the system-transform callsite moved to `session/llm/request.ts`; compaction/prompt restructured; the Config service already upstream), which was bound as folded target-API adaptations and re-rehearsed green — generation-time rehearsal catching structural drift before approval, exactly as designed. **Stages 1–4 still have no live execution** — all seven implementations predate the pipeline.
- **Pipeline-derived bindings carry real cycles end-to-end on the weak tier — three sealed in two days.** Pi 0.69.0→0.73.1 (2026-07-05/06): calibration runs 1–2 clean, real cycle sealed first-attempt in ~10 minutes with zero stumbles (side `00de664`, pin advance `130692e`, maintenance report `862d4ee` with zero findings). Cline v2.16.0→v2.17.0-cli (2026-07-06): a single Opus-4.8-low executor ran Phases 0–8 + §1.16 end-to-end in ~33 minutes with zero EXECUTOR-FAIL stumbles — the first end-to-end consumption of a pipeline-emitted binding — including the first live E2E-04 PASS; three SPEC-GAPs folded at `30ef528`; the fresh-machine install gate executed separately and PASSED (`498a0ab`), completing the parity claim's install dimension. Codex →rust-v0.125.0 (2026-07-06): plan `dde09e4` (target selected as the smallest stable tag containing the port base — version-sorted smallest-pending was a year of stale datever behind it; keyed `-on-rust-v0.125.0` per the §1.14 same-source different-target rule bound at `4c7328e`); sealed with first live E2E-04 and E2E-06 PASS, three SPEC-GAPs absorbed (fold `725a7ae`: baseline `Cargo.lock` regeneration, shallow-fork install-gate fetch depth, E2E-04's pre-first-cadence control window) and two self-corrected EXECUTOR-FAILs, none fatal; pin advance `e9a3af4`. Later releases are already detected and pending for all three git-fork harnesses (codex 0.126.0…0.142.5, cline 2.18.0-cli/3.32.6-cli, kilo 7.4.1) — they are future routine cycles, rate-limited by §1.20.
- **Escalation is formalized and the dispatcher is built.** §1.17 (`5e80849`) is a 20-code registry: every run/cycle-ending STOP records exactly one code with a machine-checkable emission condition, an invalidation scope in layer terms, and a resumption route; only `structural-*` codes enter level-2 re-derivation. `scripts/dispatch-escalation.mjs` (`3f23054`, with a 14-test suite) reads an emitted code and routes it — routine-resume with the recorded route, or re-derivation with the §2.2 entry stage and demotion set — parsing both spec tables at runtime and failing closed on any drift or ambiguity. The same commit binds the report emission format in §1.17. The dispatcher's output is a routing decision, not an authorization; owner gates still apply.
- **Release detection is automated and covers all six bound harnesses.** `scripts/detect-pending-target.mjs` (`0cbbf93`; 16-test network-free suite after the tag-suffix extension) reads the machine-readable Release detection slots bound in the spec's Part 4 — port tags vs upstream tags for the git-fork harnesses, committed evidence docs vs `npm view` for Claude Code and pi — fails closed on any anomaly, and emits a `pending-target:` line at exit 3 for a newer stable upstream. Every Stage 6 acceptance included the detector parsing the new slot unmodified (or, for cline, via the reviewed suffix extension). Two live signals became sealed cycles (Claude Code 2.1.200→2.1.201, Pi 0.69.0→0.73.1); three more are currently pending (codex, cline, kilo). The signal is a detection, not an authorization — targets stay invoker-supplied, and §1.20's cadence gate now sits between signal and cycle start.
- **Per-harness specs are split into contract vs. bindings pairs** (`bf2e390`): all six harnesses with per-harness spec documents in `docs/agent-specs/` (claude-code, cline, codex, gemini-cli, kilo, pi — OpenCode is not among them; its level-2 bindings live directly in the forward-port spec's Part 4 and `docs/opencode-e2e-runbook.md`) now have a stable contract document (obligations and posture, original filename) and a regenerable bindings document (capability evidence, verified primitives, a Binding Sites table), with contract obligations referencing bindings by key. The boundary follows the pipeline's stage boundary — bindings are Stage 1 evidence plus Stage 4 realized sites; the pipeline spec and README were updated in lockstep, and both spec-parsing scripts pass unchanged. The split's review also re-verified carried evidence against live source and corrected four stale claims — direct evidence that exploration-derived bindings rot and need re-verification cycles.
- **Standing execution steer (owner, 2026-07-05)**: doc-iteration yields are shrinking — bias work toward *executable* progress (turn spec text into exercised behavior); iterate this document only when new run evidence arrives or the recorded step list is exhausted (this iteration is the former: the codex, cline, and kilo execution evidence above).
- **Gemini-cli is the only unbound harness, and it is hard-blocked on credentials.** Stage 5 bring-up is done (side repo rebuilt, fork re-bundled and verified, CLI smoke-tested at `0.41.0-nightly`), but the live Protocol A drive needs a logged-in `~/.gemini` oauth or a `GEMINI_API_KEY`, neither of which exists, and the harness permission classifier correctly denies the working sessions any credential exploration. The owner holds the unlock; resume state is recorded in the relay chain's hand-off — the Claude Code side repo's `HAND_OFF.md`, the running log of the owner-tier session sequence that works this project.
- **Stale-evidence pressure is now measured, not hypothetical — and the answer to it is built and wired.** The contract-vs-bindings split's review corrected four stale carried claims in one pass; kilo's acceptance rehearsal caught three real upstream drifts between the port base and the pending release. The bindings re-verification pass (spec + first execution, `e8f0b68`) probed all 36 Binding Sites rows across the six bindings documents with cheap-tier read-only probes and owner-tier dispositions: 34 VERIFIED, 2 DEMOTED (pi's `gauge-channel` and `searchable-text`), demotions annotated in place with recorded follow-ups rather than silent rewrites. Cycle-plan generation now consults that freshness evidence (`fdede6a`): §1.15 opens with a bindings-freshness consultation — a DEMOTED row the plan would bind is an immediate `generation-nonconvergence` STOP resolvable only by Stage-1 re-exploration; a pass record older than the harness's last cycle start flags the bindings stale, non-blocking.
- **The routine path is chained end-to-end and the steady-state wake is defined (§1.21).** `scripts/invoke-routine-cycle.mjs` (11 tests; 56 across the four spec-parsing scripts) chains detector → cadence gate into one survey (`up-to-date` / `pending-rate-limited` / `launch-ready`), and its `--start` mode performs the invoker's cycle-start acts in one invocation: the §1.20 ledger record committed alone, the §1.18 `RUN-START` intent-log seed (an existing log fails the start closed — a resume is a relaunch act, not a new start), and a JSON launch packet carrying the reconstructed target release. The live survey works: three pending targets (codex 0.142.5, cline 3.32.6-cli, kilo 7.4.1), all rate-limited until 2026-07-13. §1.21 also binds the wake schedule: a weekly cron invocation of `scripts/routine-wake.sh` surveys and posts the result to the owner's Slack; a `launch-ready` report is the signal to start an owner-tier session that runs `--start` and drives the cycle. The one deliberately manual hop — launching that session — is an owner budget decision recorded in §1.21; the cron entry itself awaits the owner's one-line install (the session's permission classifier correctly refused crontab persistence).
- The new-harness derivation path's front half remains unexecuted: Stages 1–4 have one bespoke epic (`.agents/plans/epic-context-bonsai-agent-ports/`) and no pipeline-driven run. The tail (Stages 5–6) has four executed instances; every emitted binding that has been consumed carried its cycle clean.

## Next Step

**Operate the steady state; the next substantive work is the cadence backlog when it opens 2026-07-13.** The buildable backlog is empty: six of seven harnesses are bound, the routine path is chained signal-to-landing (§1.21), freshness evidence exists and is consulted at generation, and every remaining executable item is either rate-limited by §1.20 (no cycle may start before 2026-07-13 absent an owner announcement) or owner-blocked. Until the gate opens, the loop's defined behavior is to rest: the weekly §1.21 wake surveys and reports; no session invents work. When the gate opens, execute the backlog as routine cycles driven through the new chain (`invoke-routine-cycle.mjs --start`) — kilo v7.4.1 first (its acceptance plan already absorbed the v7.4.1 drift, and a first cycle on the newest binding is the strongest test of the pipeline's tail), then codex (→0.142.5) and cline (→3.32.6-cli), folding each harness's parked defect candidates (cline's `retrieve_same_step`; kilo's worktree-`/` in non-git dirs and per-process gauge counter) into its cycle plan. A pi cycle additionally exercises the freshness consultation's blocking path if its plan binds either DEMOTED row — Stage-1 re-exploration of `gauge-channel` and `searchable-text` is due before or during that cycle's generation.

Two owner-gated finishes sit in parallel and outrank the backlog whenever the owner unlocks them: the OpenCode v1.17.13 credentials-gate resume (authorized by relay but classifier-blocked; the owner runs the relaunch or clears the permission — mechanism in the side repo's `HAND_OFF.md`), and gemini-cli Stage 5 (resumes immediately when the owner provisions credentials). The weekly cron install for `routine-wake.sh` is likewise the owner's one-liner (§1.21).

## Provisional Future Steps

Ordered by current best guess; any or all may be replaced when the next iteration of this direction loop runs.

1. **Re-verify and re-explore demoted bindings**: Stage-1 re-exploration of pi's two DEMOTED rows, and periodic re-verification passes per `bindings-reverification-spec.md` (natural cadence: after each harness's cycle seals, since §1.16 maintenance moves binding sites).
2. **Bind gemini-cli** (Stage 5–6) when credentials arrive — the last harness, converting §4.8 to empty and completing detector coverage.
3. **Grant or decline the unattended wake hop**: if the owner authorizes scheduled owner-tier session launches, record the grant in §1.21 and extend the cron line; the chain itself needs no other change.
4. **A pipeline-driven Stages 1–4 run** remains the derivation path's unexecuted front half; no candidate harness exists today (all seven implementations predate the pipeline), so this waits for a genuinely new harness or a `structural-*` escalation that demotes an existing one into re-derivation.

## Iteration Rule For This Document

When this direction loop runs again: re-read this document, compare its Current State section against the repository, rewrite Next Step and Provisional Future Steps freely, and preserve End Goal unless the project owner approves a scope change. Supersede in place (this file, same path) so there is exactly one current direction statement.
