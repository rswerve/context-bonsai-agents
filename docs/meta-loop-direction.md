# Context Bonsai Meta-Loop: Direction

This document states the end goal for the Context Bonsai meta-loop, the next step toward it, and the provisional steps after that. It is itself an iteration artifact: the process that produced it — examine the loop's current state, absorb what recent cycles taught, restate direction — is expected to run again, and a later iteration may overwrite the "Next Step" and "Provisional Future Steps" sections wholesale. Only the End Goal section should survive iterations largely intact; treat edits to it as scope changes requiring the project owner's approval.

## End Goal

A self-updating Context Bonsai that works for every supported harness.

Concretely, the system maintains three levels of artifact, each able to regenerate the one below it:

1. **Behavior contract** (`docs/context-bonsai-agent-spec.md`) — what Context Bonsai does, harness-independent. Changes only when the product changes.
2. **Per-harness update loop** — the routine procedure that carries a port onto each new harness release. Two upstream *shapes* exist today: for *git-fork* harnesses, rebase the patch chain and revalidate; for *closed-artifact* harnesses (Claude Code), freeze the new npm artifact, re-verify semantic anchors, and revalidate. This level encodes structural assumptions about the harness (package layout, seam locations, validation commands) that hold across the majority of releases.
3. **Per-release cycle** — one execution of level 2 against one new release, ending in the e2e release gate defined by `docs/agent-specs/context-bonsai-e2e-spec.md`.

The meta-loop is the machinery that generates and regenerates level 2. It must handle three entry conditions with one pipeline, differing only in how much prior work is still trustworthy:

- **Routine release** — level 2's assumptions hold. Generate and execute a cycle plan. This is the cheap path and the common case.
- **Structural break** — a harness release invalidates level 2's assumptions. The signals already exist as fail-closed behaviors: mass classification of commits into the hard-blocking `manual_review` bucket (the cycle generator's fallback for indeterminate evidence), anchor discovery scoring below the `minScore`/`minMargin` thresholds everywhere (the behavior spec's fail-closed anchor-ambiguity rule), or a Protocol A failure (the e2e spec's secret-prune oracle) on a clean build. The failure is the escalation signal: re-derive the level-2 assumptions from the behavior contract plus fresh exploration of the new harness architecture, demoting prior bindings to untrusted priors that must be re-verified, then re-enter the routine path.
- **New harness** — no prior work exists. Run the same derivation from nothing: capability discovery, integration-posture selection (per the behavior spec's change-minimization rule), per-harness spec, implementation, e2e binding, and emission of that harness's level-2 update loop as a first-class artifact.

"Self-updating" means the routine path runs with no human decision-making, and the other two paths run agentically with human review at the existing reviewer+judge gates. Detection of which path applies is itself automatic — a path-selection dispatcher, which does not exist yet: the routine path fails closed with a structural reason code rather than grinding forward or silently succeeding.

**Executor tiering.** The determinism machinery (frozen inputs, fixed precedence, machine-checkable gates) is calibrated scaffolding, not architecture: each such rule moves a judgment call out of the executor and into the artifact, and that is precisely what lets weaker or cheaper models run the loops. The target assignment, approved by the project owner 2026-07-01: the routine path *and* its own maintenance (absorbing cycle friction back into the routine's instructions) run on a process orchestrator (`.llm-conductor/ORCHESTRATOR_AGENT.md` pattern) in the OpenCode harness on GPT-5.5 at low or medium thinking. If routine-maintenance proves beyond that executor, fall back one rung: GPT-5.5 keeps routine execution, a stronger (Fable-class) model maintains the routine. The loop above that — developing this loop system, governed by this document's Iteration Rule — runs on a stronger model with the project owner. Relaxing the determinism machinery may be explored only in tiers executed by the stronger model; in GPT-5.5-executed tiers it is load-bearing for the delegation.

## Current State (evidence)

Short SHAs below are the `SOURCE_HEAD_SHA` keys of `.agents/plans/story-rebase-cycle-<sha>.md` cycle plans, or parent-repo commits where stated.

- The routine path is now specified in one layered artifact: `docs/agent-specs/forward-port-spec.md` — shape-agnostic core, git-fork and closed-artifact shape bindings, per-harness binding slots (OpenCode and Claude Code bound; cline, codex, gemini-cli, kilo, and pi explicitly unbound). The clean-room regeneration acceptance test passed 2026-07-02: two executed cycle plans (OpenCode `4d88b95`, Claude Code `95c2422`) regenerated from the spec by Sonnet-class agents, independently audited equivalent on all four criteria, after three rounds in which the spec's own §1.15 validation loop proved to be the load-bearing scaffolding for weak-executor generation. The fail-closed machinery binds hard in practice (a regenerating agent refused offers to waive a legitimate §1.14 collision STOP; the input grammar rejected a malformed SHA).
- The GPT-5.5 pilot (the End Goal's executor-tiering experiment) has now run **four times** against upstream OpenCode v1.17.13, every run ending fail-closed with zero commits in any repo and nothing unverifiable produced. Every stumble is classified `SPEC-GAP` (the artifact left a judgment call unresolved — fix the artifact) or `EXECUTOR-FAIL` (the artifact was deterministic and the executor still failed — evidence about the tier), recorded by the non-interfering observer alongside the executor's own friction log. Run 1 (six minutes) STOPped at the §1.10 preflight on launch-environment defects: 3 SPEC-GAP, 1 minor EXECUTOR-FAIL; fixes at `85dd3b8`, validated by run 2 (no recurrence). Run 2 (~4.5 hours) STOPped at the §1.15 three-iteration cap with EXECUTOR-FAIL (deterministic-content drops unfixed) + 3 new SPEC-GAPs; §1.16 maintenance ran correctly on the STOP; owner review at `3aaea2f`. Records: `20d5d40`, `f9d9063`.
- **Run 3 (2026-07-02→03, launched on the watchdog's unchanged-terms decision at `565af44` — the watchdog being the owner's delegated supervising session, sanctioned there to decide unchanged-terms run launches on his behalf) again STOPped at the §1.15 cap — but the exhaustion anatomy inverted.** Iterations 1–2 blocked on executor plan omissions (EXECUTOR-FAIL, verdict V1), yet the executor fixed every self-caused finding within the loop budget — the first run where nothing executor-fixable remained by iteration 3. Iteration 3 blocked *only* on two new SPEC-GAPs (V2, V3): §4.2's runtime-e2e and pre-publish install gates cited generic templates but bound no concrete OpenCode commands, and the plan reviewers rightly refused invented ones — the determinism bar working as designed. None of the six previously fixed gaps recurred. The acceptance test PASSED all four clauses; §1.16 maintenance ran correctly on the STOP for the second consecutive run, and the executor's friction self-attribution matched the observer's independent classification exactly. Records at `bddcf96`; owner review adopted the executor's Part-4 edit as written at `15c0463`. Mid-run the provider quota exhausted (429, 3h22m retry-after); the process slept it out in-harness and self-resumed with zero observer contamination — external interruption, no verdict, and live validation of the ≤30-minute wake-and-verify constraint (the marker watcher would never have caught it; the wake-and-verify did).
- **Run 4 (2026-07-03, the watchdog's unchanged-terms relaunch decision at `da89bd8`) answered the isolated capability question, and the answer is no.** With V2/V3 closed and nothing left to flag, the third STOP at the §1.15 cap blocked *only* on executor-caused defects: the plan hand-rolled the install-gate URL rewrite against SSH URLs found in no owner artifact (the template's block, marked "verbatim" in `docs/opencode-e2e-runbook.md`, covers the HTTPS submodule URLs with one prefix rule), restated the bound pin-bump requirement as prose without commands, and left the bound result-recording order ambiguous through three passes. The flag-don't-invent rule (§4.2: a plan needing a command the runbook does not name must flag the missing binding, not invent one) was never legitimately triggerable and the executor correctly never flagged — its failure mode was transcription infidelity against bindings it had found and read at launch. The four-clause pilot acceptance test (seal or identified fail-closed STOP; verdicts on every stumble; §1.16 attempted and recorded in either outcome; no recurrence of previously fixed gaps) PASSed in full — none of the eight fixed gaps recurred, and §1.16 maintenance correctly made no Part 4 edit, verified by the observer (no owner review needed). Executor/observer attribution matched for the third consecutive run. A second mid-run provider 429 self-resumed in-harness at the retry-after horizon with zero observer contamination. Records at `cb0a5fb`.
- **The four-run trend**: capability question (a) — can GPT-5.5 generate a validation-clean plan within the §1.15 budget — now has its first clean test and it failed, reversing run 3's tentative positive signal. Runs 2–4 are 0-for-3 at the cap, and run 4 removes the last confound: run 3 showed the executor fixing all self-caused findings when spec gaps consumed the budget, but run 4, with no gap taxing the loop, still exhausted it on self-caused defects. Question (b), routine self-maintenance, is a **thrice-confirmed yes** — correct Part-4-only discipline, correct escalation split, attribution matching the observer's, in all three STOP outcomes that reached it.
- **The run-3 gaps are closed** (`2f8495a`): `docs/opencode-e2e-runbook.md` now binds the concrete commands for both gates — every command marked EXECUTED (grounded in the plugin repo's recorded 2026-03-02 and 2026-02-27 test runs), SOURCE-VERIFIED (fork source at cited lines), or COMPOSED (assembled from those primitives; first end-to-end run is the next cycle's gate, and a composed-command failure is a finding against the runbook). §4.2 carries the durable slot-gap rule the run-3 maintenance edit introduced: a plan needing a command the runbook does not name must flag the missing binding, not invent — converting that whole class of §1.15 exhaustion into an explicit early STOP.
- Run-2 operational data still stands: §1.15 iteration wall-clock varies by orders of magnitude (~4h15m vs under three minutes), so cycle cadence is a scheduling problem independent of capability; the ≤30-minute wake-and-verify constraint (project owner, 2026-07-02, recorded in the meta-loop hand-off intent rules) binds all sub-owner layers and has now caught and correctly ridden out two quota stalls (runs 3 and 4).
- The spec has still survived zero live cycles: all four pilot runs halted before replay began. Upstream v1.17.13 remains the live-test occasion. Run-4 residue (`.agents/plans/story-rebase-cycle-0dfbeed….md` + three validation JSONs — same `SOURCE_HEAD_SHA` as run 3's, since the upstream target is unchanged) sits untracked in the parent tree and must be archived then cleared before any run 5, or §1.14 correctly halts it (run-2/run-3 precedent: copy under `.agents/pilot/archive/`, then delete).
- Escalation is still scattered: no enumerated reason codes, no path-selection dispatcher. Four live STOP records now exist in two non-structural classes — launch-environment (run 1) and §1.15 non-convergence (runs 2–4) — and the flag-don't-invent rule is effectively the first slot-level escalation condition stated in the spec itself, now validated in the negative direction (run 4 correctly did not flag when nothing was missing).
- The new-harness derivation path and the per-harness contract/bindings split are unchanged: one bespoke epic (`.agents/plans/epic-context-bonsai-agent-ports/`), no reusable onboarding procedure extracted.

## Next Step

Decide the plan-generation tiering question. Run 4 delivered the clean EXECUTOR-FAIL that runs 2–3 could not: with every needed binding present and nothing to flag, GPT-5.5 still exhausted the §1.15 budget on self-caused transcription defects. The evidence now splits the routine path's two halves — plan *generation* (question (a): first clean test, failed) and cycle *maintenance* (question (b): thrice-confirmed yes) — and the supported branch (1 below) amends the End Goal's tiering paragraph, which makes this the owner's decision alone; the `565af44` watchdog delegation covered unchanged-terms run launches, not scope changes.

**Decision status: OPEN — awaiting the owner.** Branches, with the drafter's labeled read:

1. **Split plan generation up-tier** (a stronger model generates and validates the cycle plan; GPT-5.5 executes the approved plan and keeps routine maintenance) — *supported*. Runs 2–4 are 0-for-3 at the §1.15 cap, run 4 unconfounded; maintenance keeps passing, so only the generation half moves. Amends the End Goal's tiering paragraph (owner approval inherent). If adopted, the immediate concrete action re-enters the v1.17.13 cycle with a stronger-model-generated plan — which would also finally take `forward-port-spec.md` into its first live replay.
2. **Run 5 under unchanged terms, for replication** — *defensible but not required by the evidence*. A second clean exhaustion would harden the attribution; the cost is another multi-hour cycle against a target that has now absorbed four, and the isolation conditions cannot get cleaner than run 4's. Watchdog-delegable under the existing precedent if the owner prefers it.
3. **Artifact-side hardening without re-tiering** (e.g. a bound plan skeleton the executor fills, pushing §1.15 further toward transcription) — *weakened by run 4's specifics*: the failing content was already bound verbatim and the executor altered it anyway. More artifact cannot compensate for infidelity to the artifact.

The fallback rung as written (GPT-5.5 executes, Fable maintains) remains unsupported in its own terms — maintenance is the half the pilot keeps confirming; the ladder inverted.

Acceptance test for this Next Step: the tiering decision is recorded here with owner provenance; if branch 1, the End Goal's tiering paragraph is amended in the same change; if any branch re-enters the cycle, the run-4 residue is verifiably archived and cleared first (§1.14).

## Provisional Future Steps

Ordered by current best guess; any or all may be replaced when the next iteration of this direction loop runs.

1. **Formalize escalation**: enumerate the structural-break reason codes the routine path can emit — mined from the STOP/hard-fail points that already exist in `forward-port-spec.md` and the behavior contract, not invented — and specify for each its machine-checkable emission condition and its invalidation scope in the spec's own layer terms. The four pilot runs have contributed two non-structural classes (launch-environment; §1.15 non-convergence) and one nascent slot-level code (the §4.2 missing-binding flag, the first escalation condition the spec states inline); the scope taxonomy must carry these alongside the structural-break scopes, since conflating them would route environment or executor defects into expensive level-2 re-derivation. This is the input contract for the path-selection dispatcher.
2. **Refine §1.15 exhaustion semantics**: the 3-iteration cap counts rounds, not convergence. The missing-binding flag now prevents the known-gap class from reaching exhaustion at all, but the residual ambiguity stands: an exhausted loop still cannot say whether the same findings persisted across rounds (executor cannot converge) or each round surfaced new ones (run 4's anatomy: each pass surfaced fresh executor-caused defects). Make the loop's exit evidence record which case occurred, so attribution and any escalation code built on this STOP read correctly. Parts 1–3 change; owner approval.
3. **Specify the derivation pipeline** (the structural-break / new-harness path): capability discovery against the behavior contract, integration-posture selection, per-harness spec generation, e2e slot binding, and emission of the harness's level-2 bindings. Mine `.agents/plans/epic-context-bonsai-agent-ports/` and the per-harness specs' shared section structure for the stage contracts. The escalation codes from step 1 define this pipeline's entry conditions.
4. **Split per-harness specs into contract vs. bindings.** The stable behavioral content and the regenerable structural facts currently share one document; the derivation pipeline needs to know what it may rewrite.
5. **Reduce human steps in the routine path** — automated release detection per harness, automatic cycle-plan generation on detection — only after `forward-port-spec.md` has survived at least one live cycle.

## Iteration Rule For This Document

When this direction loop runs again: re-read this document, compare its Current State section against the repository, rewrite Next Step and Provisional Future Steps freely, and preserve End Goal unless the project owner approves a scope change. Supersede in place (this file, same path) so there is exactly one current direction statement.
