# Context Bonsai Meta-Loop: Direction

This document states the end goal for the Context Bonsai meta-loop, the next step toward it, and the provisional steps after that. It is itself an iteration artifact: the process that produced it — examine the loop's current state, absorb what recent cycles taught, restate direction — is expected to run again, and a later iteration may overwrite the "Next Step" and "Provisional Future Steps" sections wholesale. Only the End Goal section should survive iterations largely intact; treat edits to it as scope changes requiring the project owner's approval.

## End Goal

A self-updating Context Bonsai that works for every supported harness.

Concretely, the system maintains three levels of artifact, each able to regenerate the one below it:

1. **Behavior contract** (`docs/context-bonsai-agent-spec.md`) — what Context Bonsai does, harness-independent. Changes only when the product changes.
2. **Per-harness update loop** — the routine procedure that carries a port onto each new harness release. Two upstream *shapes* exist today: for *git-fork* harnesses, rebase the patch chain and revalidate; for *closed-artifact* harnesses (Claude Code), freeze the new npm artifact, re-verify semantic anchors, and revalidate. This level encodes structural assumptions about the harness (package layout, seam locations, validation commands) that hold across the majority of releases.
3. **Per-release cycle** — one execution of level 2 against one new release, ending in the e2e release gate defined by `docs/agent-specs/context-bonsai-e2e-spec.md`.

The meta-loop is the machinery that generates and regenerates level 2. It must handle three entry conditions with one pipeline, differing only in how much prior work is still trustworthy:

- **Routine release** — level 2's assumptions hold. Generate and execute a cycle plan. This is the cheap path and the common case.
- **Structural break** — a harness release invalidates level 2's assumptions. The signals already exist as fail-closed behaviors: mass classification of commits into the hard-blocking `manual_review` bucket (the cycle generator's fallback for indeterminate evidence), anchor discovery scoring below the `minScore`/`minMargin` thresholds everywhere (the behavior spec's fail-closed anchor-ambiguity rule), or a Protocol A failure (the e2e spec's secret-prune oracle) on a clean build. The failure is the escalation signal: re-derive the level-2 assumptions from the behavior contract plus fresh exploration of the new harness architecture, demoting prior bindings to untrusted priors that must be re-verified, then re-enter the routine path.
- **New harness** — no prior work exists. Run the same derivation from nothing: capability discovery, integration-posture selection (per the behavior spec's change-minimization rule), per-harness spec, implementation, e2e binding, and emission of that harness's level-2 update loop as a first-class artifact.

"Self-updating" means the routine path runs with no human decision-making, and the other two paths run agentically with human review at the existing reviewer+judge gates. Detection of which path applies is itself automatic — a path-selection dispatcher, which does not exist yet: the routine path fails closed with a structural reason code rather than grinding forward or silently succeeding.

**Executor tiering.** The determinism machinery (frozen inputs, fixed precedence, machine-checkable gates) is calibrated scaffolding, not architecture: each such rule moves a judgment call out of the executor and into the artifact, and that is precisely what lets weaker or cheaper models run the loops. The target assignment, approved by the project owner 2026-07-01: the routine path *and* its own maintenance (absorbing cycle friction back into the routine's instructions) run on a process orchestrator (`.llm-conductor/ORCHESTRATOR_AGENT.md` pattern) in the OpenCode harness on GPT-5.5 at low or medium thinking. If routine-maintenance proves beyond that executor, fall back one rung: GPT-5.5 keeps routine execution, a stronger (Fable-class) model maintains the routine. The loop above that — developing this loop system, governed by this document's Iteration Rule — runs on a stronger model with the project owner. Relaxing the determinism machinery may be explored only in tiers executed by the stronger model; in GPT-5.5-executed tiers it is load-bearing for the delegation.

## Current State (evidence)

Short SHAs below are the `SOURCE_HEAD_SHA` keys of `.agents/plans/story-rebase-cycle-<sha>.md` cycle plans.

- The routine path is now specified in one layered artifact: `docs/agent-specs/forward-port-spec.md` — shape-agnostic core, git-fork and closed-artifact shape bindings, per-harness binding slots (OpenCode and Claude Code bound; cline, codex, gemini-cli, kilo, and pi explicitly unbound). Committed at `1fedcd9`; the meta-plan it restructured (`.agents/plans/story-meta-plan-for-future-rebase-planning.md`) is superseded by a pointer note at that path. The previous iteration's Next Step is complete, and the e2e spec's once-dangling `forward-port-spec.md` citation is now real.
- The previous Next Step's acceptance test — clean-room regeneration — passed 2026-07-02: the two most recent executed cycle plans (OpenCode `4d88b95`, Claude Code `95c2422`) were regenerated from the spec by Sonnet-class agents and independently audited equivalent on all four criteria (seal/blocking gates, bucket taxonomy and precedence, validation command set with working directories, immutable e2e scope). It took three rounds: rounds 1–2 surfaced over a dozen spec fixes, each grounded in the executed artifacts and folded in; round 3 closed recurring executor transfer drops only by running the spec's own §1.15 validation loop. That is direct evidence the validation loop — not seal-gate checklists alone — is the load-bearing scaffolding for weak-executor generation; carry it into the GPT-5.5 pilot (the executor-tiering experiment — see Next Step) as non-negotiable.
- The fail-closed machinery binds hard in practice. During the regeneration test, one regenerating agent hit the spec's §1.14 same-cycle collision STOP, independently verified the cycle had already run, and refused the invoking agent's offers to waive the STOP; the strict input grammar rejected a malformed 39-character SHA in a task brief. Consequence: historical-regeneration exercises must be framed as document production, not cycle execution, or the collision STOP correctly halts them.
- One post-acceptance spec gap was found and fixed in place: the OpenCode slot's e2e plugin wiring pointed at "the pinned harness's copy" of `.opencode/opencode.jsonc`, but the pinned copy carries no `"plugin"` key (the edit the spec assumed existed only transiently). The wiring content is now stated explicitly in `forward-port-spec.md` §4.2; the project owner confirmed per-cycle regeneration of the wiring (worktree-local, uncommitted) as the intended design.
- The GPT-5.5 pilot (the End Goal's executor-tiering experiment) has run twice against upstream OpenCode v1.17.13, both runs ending fail-closed with zero commits in any repo and nothing unverifiable produced — the determinism machinery held under a weak executor both times. Every pilot stumble is classified `SPEC-GAP` (the spec or brief left a judgment call unresolved — fix the artifact) or `EXECUTOR-FAIL` (the artifact was deterministic and the executor still failed — evidence about the tier), recorded by the observer — the stronger-model session that watches a run without interfering — in an observer log alongside the executor's own friction log. Run 1 (2026-07-02, six minutes) STOPped at the §1.10 clean-state preflight on launch-environment defects: 3 SPEC-GAP, 1 minor self-recovered EXECUTOR-FAIL, per the observer log committed at `20d5d40`. The four owner-tier fixes landed at `85dd3b8`; run 2 exercised all three fixed SPEC-GAPs and none recurred, so those fixes are validated.
- Run 2 (2026-07-02, ~4.5 hours) STOPped at §1.15: the plan-validation loop exhausted its 3 iterations with blocking reviewer findings still open, so the cycle proper — replay, seal, publish — never started. §1.16 — the routine self-maintenance step, which absorbs cycle friction back into the spec — then ran on the STOP exactly as the fixed spec requires, producing the executor's own failure attribution and a disciplined spec edit confined to Part 4 (the per-harness binding slots, the only layer §1.16 lets the routine executor edit; the shape-agnostic core and shape bindings of Parts 1–3 escalate to the owner tier) closing the three SPEC-GAPs it hit (detached-plugin publish handling, parent pin advance versus the worktree-occupied replay branch, install-gate record disposition). The re-run acceptance test passed on all four clauses; records committed `f9d9063`. Owner review of the executor's spec edit, committed `3aaea2f`: two fixes adopted as written, the install-gate disposition amended to bind to the install template's Result Recording — records land in the port side repo (`opencode_context_bonsai_plugin/docs/install-e2e-results-<DATE>.md`, mirroring the Claude Code port's committed `docs/e2e-results-*.md` precedent) rather than in the pilot's own directory, which routine cycles won't have.
- Run 2 answers the pilot's two capability questions — (a) can GPT-5.5 execute the routine per-release cycle, and (b) can it maintain the routine's own instructions from cycle friction — asymmetrically, and in the opposite direction from the End Goal's fallback rung. Question (b), routine self-maintenance: **yes** — the Part-4-only edit discipline, the slot-fix vs core-gap escalation split, and the attribution verdicts were all correct; the observer concurred with the executor's own attribution. Question (a), routine execution: **partial** — frozen inputs, gate discipline, and STOP behavior were flawless, but plan generation never converged: content the spec states verbatim (bootstrap command, README content probe, release-ladder commands, scope checks) was still missing or wrong after three validation iterations — an EXECUTOR-FAIL. One confound softens the reading: iteration 3's blocking findings were mostly the three SPEC-GAPs, so part of the fixed loop budget went to surfacing artifact defects rather than executor error. Iterations 1–2's findings, however, were deterministic-content drops with no spec gap involved.
- Run 2 also produced executor-viability operational data: §1.15 iteration 2 took ~4h15m of wall-clock for one revise-and-review round (iteration 3 took under three minutes), so a full cycle at that cadence is a scheduling problem independent of capability. And the §1.15 cap counts rounds, not convergence — cap-exhaustion currently conflates "executor cannot converge" with "spec gaps surfacing late," which matters for reading any future STOP at that gate.
- A process constraint is now binding on all meta-loop work (project owner, 2026-07-02, recorded in the meta-loop hand-off — `tweakcc_context_bonsai/HAND_OFF.md`, intent rules): no layer below the owner's top-level session may wait on anything — subagent, watcher, monitor — longer than 30 minutes without waking to verify actual state, and every watcher must be verified against the channel it watches before being trusted. Run 2 violated both in practice: the observer's exit watcher grepped a log the run's exit marker never entered (the marker printed only to the tmux pane), and the orchestrator's subagent wait sat ~4h silent. Both must be fixed before any third run.
- The spec has still survived zero live cycles: both pilot runs halted before replay began. Upstream v1.17.13 (published 2026-07-01, two minor versions past the pinned v1.15.7) remains the live-test occasion, per the project owner's 2026-07-02 decision to pilot straight onto it and treat mid-cycle STOP risk as part of the test.
- Escalation is still scattered: structural-break signals exist as individual fail-closed STOPs in the spec and behavior contract, with no enumerated reason codes and no path-selection dispatcher. Two live STOP records now exist as input, in two distinct classes: run 1's launch-environment class (defect in the invoking setup, no upstream assumption impugned) and run 2's plan-generation non-convergence (impugns the executor tier or the §1.15 loop budget — also not structural-break, since no level-2 upstream assumption failed).
- The new-harness derivation path and the per-harness contract/bindings split are unchanged: one bespoke epic (`.agents/plans/epic-context-bonsai-agent-ports/`), no reusable onboarding procedure extracted, per-harness specs still mixing stable behavioral contract with regenerable structural facts.

## Next Step

Obtain the project owner's tiering decision on the run-2 evidence, and close the launch defects that every decision branch needs fixed.

The pilot's committed terms are spent: two runs, both fail-closed, the run-1 fixes validated by run 2, and the two capability questions answered asymmetrically (maintenance yes, execution partial — see Current State). What to do with that evidence is a tiering decision, which the project owner has explicitly reserved (2026-07-02); this iteration's job is to frame it, not make it. The evidence-supported branches:

1. **Run 3 under unchanged terms.** The cleanest response to the confound: with the three §4.2 gaps now closed, the §1.15 loop budget is no longer taxed by artifact defects, so a re-run isolates pure plan-generation capability. The counter-evidence is iterations 1–2, whose blocking findings were deterministic-content drops with no spec gap involved — a run 3 that exhausts the loop again on the same class of drop would settle question (a) as a clean EXECUTOR-FAIL.
2. **Split plan generation up-tier.** A stronger model generates and validates the cycle plan; GPT-5.5 executes it and keeps §1.16 maintenance, which run 2 demonstrated it can do. This is the inverse of the End Goal's anticipated fallback rung ("GPT-5.5 keeps routine execution, a stronger model maintains"), so adopting it amends the End Goal's tiering paragraph — a scope change requiring the owner's approval in its own right.
3. **The fallback rung as written.** Not supported: it was premised on maintenance failing, and maintenance is the question run 2 answered *yes*.

Launch prep owed under every branch, all owner-tier work grounded in the run-2 records (doable before the decision):

1. **Run residue**: run 2's draft plan and validation artifacts (`.agents/plans/story-rebase-cycle-0dfbeed….md`, `.agents/plans/validation/*-0dfbeed….json`) sit uncommitted in the parent tree; the §1.14 same-cycle collision STOP will correctly halt any run 3 against v1.17.13 unless they are cleared first — run 1's residue was deleted before run 2 launched, and the same is owed here. Decide clearing vs per-run naming once, since run 2 also silently overwrote run 1's on-disk friction log and final report (history preserves both).
2. **Exit-signal plumbing**: the run harness must tee its `PILOT-PROCESS-EXITED` marker into the watched run log, and the observer must verify marker-reaches-channel before trusting a watcher (run 2's watcher could never fire).
3. **Wait ceilings**: cap the orchestrator's subagent wait and every observer watcher at ≤30 minutes with wake-and-verify, per the owner's 2026-07-02 process constraint (run 2's iteration-2 wait ran ~4h silent).
4. **§1.16 maintenance-edit disposition**: the spec doesn't say what the executor does with its own Part-4 edit; run 2 left it uncommitted in the working tree, which worked only because the owner tier picked it up the same day and which would trip a later cycle's §1.10 preflight. Specify the disposition (likely: leave uncommitted and name it in the final report for owner review; enumerate it as expected dirt for any run that follows before review). This is a Parts 1–3 edit — owner-tier by definition.

Acceptance test: the owner's tiering decision is recorded in this document's next iteration (any of the three branches, or one not listed), and the four launch-prep items are verifiably closed — residue cleared or named, exit marker demonstrated in the watched channel, wait caps in the launch procedure, maintenance-edit disposition written into §1.16.

## Provisional Future Steps

Ordered by current best guess; any or all may be replaced when the next iteration of this direction loop runs.

1. **Formalize escalation**: enumerate the structural-break reason codes the routine path can emit — mined from the STOP/hard-fail points that already exist in `forward-port-spec.md` and the behavior contract, not invented — and specify for each its machine-checkable emission condition and its invalidation scope in the spec's own layer terms (per-harness binding slot, shape binding, or the per-harness spec content those bindings were derived from). Record the codes in the spec itself so executors emit them without cross-document reads. STOPs the pilot actually hits are first-class input; the two runs have contributed two distinct non-structural classes — run 1's launch-environment STOP (defect in the invoking setup, no upstream assumption impugned) and run 2's §1.15 non-convergence STOP (impugns the executor tier or loop budget) — and the scope taxonomy must carry both alongside the structural-break scopes, since conflating them would route environment or executor defects into expensive level-2 re-derivation. This is the input contract for the path-selection dispatcher.
2. **Refine §1.15 exhaustion semantics**: the 3-iteration cap counts rounds, not convergence, so its STOP cannot distinguish "same findings unfixed across rounds" (executor cannot converge) from "each round surfaces new findings" (the plan is converging while the spec sheds late gaps — run 2's iteration 3 was mostly the latter). Make the loop's exit evidence record which case occurred, so attribution and any escalation code built on this STOP read correctly. Parts 1–3 change; owner approval.
3. **Specify the derivation pipeline** (the structural-break / new-harness path): capability discovery against the behavior contract, integration-posture selection, per-harness spec generation, e2e slot binding, and emission of the harness's level-2 bindings. Mine `.agents/plans/epic-context-bonsai-agent-ports/` and the per-harness specs' shared section structure (Capability Evidence Matrix, Verified Host Primitives, Integration Posture) for the stage contracts. The escalation codes from step 1 define this pipeline's entry conditions.
4. **Split per-harness specs into contract vs. bindings.** The stable behavioral content and the regenerable structural facts currently share one document (e.g. `docs/agent-specs/claude-code-context-bonsai-spec.md` mixes both). The derivation pipeline needs to know what it may rewrite.
5. **Reduce human steps in the routine path** — automated release detection per harness, automatic cycle-plan generation on detection — only after `forward-port-spec.md` has survived at least one live cycle.

## Iteration Rule For This Document

When this direction loop runs again: re-read this document, compare its Current State section against the repository, rewrite Next Step and Provisional Future Steps freely, and preserve End Goal unless the project owner approves a scope change. Supersede in place (this file, same path) so there is exactly one current direction statement.
