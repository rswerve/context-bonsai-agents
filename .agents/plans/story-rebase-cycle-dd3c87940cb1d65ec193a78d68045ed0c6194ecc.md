# Story: Forward-port cycle — Pi Context Bonsai onto @mariozechner/pi-coding-agent 0.73.1

Routine forward-port cycle per `docs/agent-specs/forward-port-spec.md` (Part 1 core, Part 3 shape binding — pure-extension variant, §4.4 Pi slot table). Generated by the owner tier on 2026-07-05 from the spec plus the §4.4 slots; this is the first routine cycle for the Pi harness and the Stage 6 acceptance-gate plan (`derivation-pipeline-spec.md` §9). All relative paths are from the parent repo root (`context-bonsai-agents/`); side-repo commands state their working directory.

## 1. Goal, Non-Goal, execution outcome

**Goal**: carry the Pi port from pinned upstream 0.69.0 to 0.73.1 — bump the pin sites, re-verify every binding-site anchor against the frozen target, and certify the result through the slot's validation and e2e gates.

**Non-Goal**: no extension behavior changes, no new features, no edits to the `pi/` fork submodule's tree, no spec edits (gate 12), nothing pushed anywhere.

**Execution outcome**: `pi_context_bonsai` HEAD advances by exactly one replay commit on its tracked branch containing the approved change set (pin bumps + cycle evidence docs); the parent gains one pin-advance commit (§3.8); all seal gates 1–14 pass. Everything stays local — outward actions are owner-gated and out of scope.

## 2. Frozen inputs (§1.1)

| Input | Frozen value |
|---|---|
| `SOURCE_HEAD_SHA` (side repo `pi_context_bonsai`) | `dd3c87940cb1d65ec193a78d68045ed0c6194ecc` |
| `SOURCE_REF` | `refs/heads/main` (side repo tracked branch) |
| `frozen_target_package` | `@mariozechner/pi-coding-agent@0.73.1` |
| npm `dist.integrity` | `sha512-gXQh3SaZmWTfVMc4Ao5+LGbVeKvzyO7tolok0nLsZgq9nGjZx/EEU3NM8C+qUnB4Nvs2rswG5qOVgLzQkq0fHQ==` |
| npm `dist.shasum` | `a14f26f45d3553556149432d743242675771862a` |
| Companion pins (frozen same way) | `@mariozechner/pi-agent-core@0.73.1`, `@mariozechner/pi-ai@0.73.1` |
| Extraction manifest (§3.1) | `/tmp/pi-bonsai-artifacts/pi/0.73.1/manifest.json`; extracted tree `/tmp/pi-bonsai-artifacts/pi/0.73.1/package`; tarball sha256 `7bf5d492670c04fd7c599dee7e6eaabff964084affd216766107e6741df7a2e1` |
| Upstream source tag (drift-scan corroboration) | `v0.73.1` = `781152fc24841dc54b22284514604048ebe5e2c9`, fetched into the `pi/` submodule |
| Validation mode (§1.11) | `committed-final` |
| Supersession lineage | none — first Pi cycle |

There is no patched live install in this shape's pure-extension variant: the runtime under test is the target version the e2e harness npm-installs fresh per run (§4.4 Upstream identity). The runtime binding is asserted in Phase 4 (the `PI_PKG` bump) and Phase 6 (the harness's install of exactly `@mariozechner/pi-coding-agent@0.73.1`, visible in its install log).

## 3. Allowlists, planned targets, explicit non-targets

Planned target files, all in `pi_context_bonsai/` (§4.4 scope policy):

- `package.json` — dependencies `@mariozechner/pi-agent-core` and `@mariozechner/pi-coding-agent` `0.69.0` → `0.73.1`; devDependency `@mariozechner/pi-ai` `0.69.0` → `0.73.1`; mirrored `version` field `0.69.0` → `0.73.1`. In scope only for these four fields.
- `package-lock.json` — regenerated by `npm install`; never hand-edited.
- `test/e2e/run-e2e.sh` — the single line `PI_PKG="@mariozechner/pi-coding-agent@0.69.0"` → `...@0.73.1`. No other edit.
- `docs/binding-verification-0.73.1.md` — new file, the cycle's ported-version evidence doc (§4.4 Release detection): frozen target identity, pin sites, drift-scan outcome, validation and Protocol A evidence. Modeled on `docs/binding-verification-0.69.0.md`.
- `docs/e2e-testing.md` — one appended Test Runs row for this cycle's Protocol A run. Append-only.

Explicit non-targets (§4.4): `docs/agent-specs/forward-port-spec.md`; the `pi/` fork submodule's working tree; `opencode/`, `tweakcc_context_bonsai/`, every other harness repo; `~/.pi/**` auth and live user-global extensions (the automated harness's own documented staging/removal of `~/.pi/agent/extensions/context-bonsai` is the sole exception; it refuses to clobber a pre-existing directory); prior cycles' plans; extracted artifacts or manifests inside any repository; `pi_context_bonsai/src/**` and `test/**` code (all anchors classified `unchanged_anchor` — a needed code edit would be an out-of-scope fixup requiring a §1.7 exception recorded before the edit).

## 4. Classification (§1.2–§1.3, §3.2–§3.4)

The inventory is the §4.4 seam/anchor registry: the 12 items of the Binding Sites table plus the credential-discovery surface in `docs/agent-specs/pi-context-bonsai-bindings.md`. The committed replay-set artifact is the classification record (§1.14 item 4, closed-artifact form):

- **Artifact**: `.agents/plans/validation/replay-set-dd3c87940cb1d65ec193a78d68045ed0c6194ecc.json` (12 rows, sorted by `anchor_id`; §3.4 field order; committed and diff-tracked — no checksum, per §1.4's Part 3 binding).
- **Outcome**: 11 × `unchanged_anchor`, 1 × `docs_evidence_only` (`credential-discovery` — line positions moved in v0.73.1's `auth-storage.ts`; behavior identical; the citation qualifier already landed in the parent bindings doc at generation). Zero `updated_anchor`, zero hard-blocking rows — no §1.5 approvals needed beyond this plan's own approval citation, which every row carries via this section.
- Drift-scan evidence: independent repository-inspecting scan, 2026-07-05, against both the fetched `v0.73.1` tag source and the extracted published package; per-row citations in the artifact's `rationale`/`evidence_ref` fields.

## 5. Shape-required analysis: anchor re-derivation records (§1.14 item 5)

No anchor required re-derivation. The one non-`unchanged` row (`credential-discovery`) is evidence-only: v0.73.1 inserts `AuthStorage.getAuthStatus()`, shifting `hasAuth` to `auth-storage.ts:331`, `inMemory` to `:210`, and the `getApiKey` priority docs to `:446-451`, with logic, names, and priority order identical. Changes observed near the seams but not affecting them, recorded for the §1.16 report: new `shouldStopAfterTurn` agent-loop hook; new `message_end` extension event (a second potential transcript-mutation surface — not consumed); agent transport default `"sse"` → `"auto"`; provider catalog churn in `ai/types.ts`; `session-manager.ts` has zero diff between the tags.

## 6. Acceptance criteria

1. All five planned target files changed exactly as scoped; nothing else in the realized diff (seal gate 9).
2. Post-replay `npm test` and `npm run typecheck` green from `pi_context_bonsai/` at the bumped pins (gate 8; baseline must-be-green rows).
3. Protocol A (E2E-07) PASS via `test/e2e/run-e2e.sh` — which installs exactly the frozen target — plus the E2E-03 retrieve roundtrip and E2E-01 placeholder-half drives per `docs/pi-e2e-runbook.md` (gate 11; §4.4 required minimum; no `updated_anchor` rows, so no additional live drives are pulled in).
4. `docs/binding-verification-0.73.1.md` exists in the replay commit and `node scripts/detect-pending-target.mjs --harness Pi` then reports `up-to-date` (exit 0) — the Release detection slot advances with the cycle.
5. Side-repo commit precedes the parent pin advance; final verification green in both repos (§3.8, gate 14).
6. Zero unresolved exception records; spec untouched (gates 10, 12).

## 7. Implementation phases

Every phase-ending STOP records exactly one §1.17 reason code (`escalation-reason-code: <code>` line) in the final report and the §1.16 maintenance report. **This run's executor is below the owner tier — a frozen fact of this plan, not an executor judgment — so §1.18 binds in full**: the launcher seeds an intent log with a `RUN-START` UTC timestamp and names its path in the launch context; the intent log lives outside both repos (e.g. under `/tmp/pi-bonsai-e2e/`). **If no intent-log path is named, STOP immediately** — report the missing §1.18 binding to the invoker and do not proceed (do not evaluate tier, do not improvise a path). Append an intent-log line before every side-effecting action; timestamps command-sourced via `$(date -u +%FT%TZ)`; reconcile from evidence at every phase start and before any STOP.

**Phase 0 — credentials preflight** (§1.12, §4.4 Credentials): `test -n "$BONSAI_E2E_PROVIDER" && test -n "$BONSAI_E2E_MODEL"`. On failure: STOP, `escalation-reason-code: input-credentials-missing`. Deeper credential validity is gated inside the harness by Pi's own `AuthStorage.hasAuth` (`test/e2e/check-credentials.ts`); a Phase 6 in-harness credential failure is a per-scenario `BLOCKED`, not a plan-wide hard fail.

**Phase 1 — bootstrap and frozen-identity verification** (§1.12, §1.1):
- Toolchain: `node --version` (≥ 20), `npm --version`, `jq --version`, `sha256sum --version` all succeed; else STOP `env-toolchain-missing`.
- From parent root: `git -C pi_context_bonsai rev-parse HEAD` must equal `dd3c87940cb1d65ec193a78d68045ed0c6194ecc`. If not: STOP `input-source-drift` (§1.9 — generate a fresh plan; never reset the branch).
- Re-verify the frozen npm identity: `npm view @mariozechner/pi-coding-agent@0.73.1 dist.integrity` must print the frozen sha512 above; else STOP `executor-artifact-integrity`.
- Verify the extraction manifest exists and matches: `jq -r .tarball_sha256 /tmp/pi-bonsai-artifacts/pi/0.73.1/manifest.json` = frozen tarball sha256, and `sha256sum /tmp/pi-bonsai-artifacts/pi/0.73.1/mariozechner-pi-coding-agent-0.73.1.tgz` reproduces it. If the artifact storage is missing (a `/tmp` wipe), recreate it exactly per the manifest's `extraction_command` from `/tmp/pi-bonsai-artifacts/pi/0.73.1/` and re-verify the sha256 against the frozen value; a mismatch after recreation is STOP `executor-artifact-integrity`.
- Hydration: from `pi_context_bonsai/`: `test -d node_modules || npm install`.

**Phase 2 — workspace preflight** (§1.10, §3.5): work happens in place in `pi_context_bonsai/` on its tracked branch. Launch precondition: the §1.14 item-11 commit adding this plan and the replay-set artifact has already landed in the parent — if `git status --short` shows either file as untracked or modified, STOP `env-workspace-dirty` (the launcher must commit them first). `git -C pi_context_bonsai status --short` must be empty. From parent root, `git status --short` must contain only the enumerated pre-existing dirty paths: ` M tweakcc_context_bonsai` and ` M pi_context_bonsai` (submodule-pin dirt from local-only bookkeeping; untouchable except this cycle's own Phase 7 pin advance) — plus, from Phase 3 onward, the untracked baseline artifact this cycle creates (committed at Phase 7). Any other dirty path: STOP `env-workspace-dirty`. Worktree artifact check (§1.14 item 10), recorded at generation and re-run before the first edit: for each planned target file, overlap against `tracked-dirty` and `existing-untracked` classes — at generation time all five were clean/absent (`docs/binding-verification-0.73.1.md` does not exist; the other four are tracked and unmodified). Any overlap at execution time blocks that path until dispositioned with a decision citation.

**Phase 3 — baseline capture** (§1.6, §4.4 Baseline rows; artifact `.agents/plans/validation/baseline-dd3c87940cb1d65ec193a78d68045ed0c6194ecc.json`, row fields `row_id, command, frozen_target_package, frozen_source_head_sha, exit_code, result, artifact_path, provenance_ref`; post-replay and e2e result rows are appended to the same file per the executed Claude Code convention. The executor creates the file as a JSON array of row objects with exactly the fields above, in that order — the committed `baseline-*.json` precedents are the shape reference; post-replay rows reuse the baseline `row_id` prefixed `p` (`p02` for the post-replay `npm test`, `p03` for typecheck), e2e rows use `e01`-style ids):
- b01, from `pi_context_bonsai/`: `npm install`
- b02 (must-be-green), from `pi_context_bonsai/`: `npm test`
- b03 (must-be-green), from `pi_context_bonsai/`: `npm run typecheck`
- b04, from `pi_context_bonsai/`: `test ! -f docs/binding-verification-0.73.1.md && echo missing-as-expected || (echo unexpected-presence && false)` — `unexpected-presence` means this cycle already ran: STOP `input-cycle-already-generated` (§1.14; but first apply the §1.18 clean-start rule — evidence postdating `RUN-START` is this run's own work and means resume, not collision).
- A must-be-green failure at baseline (pre-bump pins — this is the port's own current state, not clean upstream): STOP `generation-plan-defect` and report; do not fix unrelated breakage in-cycle.

**Phase 4 — replay** (§3.5; one commit at the end of Phase 6):
1. In `pi_context_bonsai/package.json`, set exactly: dependencies `@mariozechner/pi-agent-core` = `0.73.1`, `@mariozechner/pi-coding-agent` = `0.73.1`; devDependencies `@mariozechner/pi-ai` = `0.73.1`; `version` = `0.73.1`.
2. In `pi_context_bonsai/test/e2e/run-e2e.sh`, set `PI_PKG="@mariozechner/pi-coding-agent@0.73.1"`.
3. From `pi_context_bonsai/`: `npm install` (regenerates `package-lock.json` at the new pins).
4. Write `pi_context_bonsai/docs/binding-verification-0.73.1.md` on the 0.69.0 doc's structure: frozen identity (§2 values above), pin sites, the drift-scan outcome (11 `unchanged_anchor` + 1 `docs_evidence_only`, citing the replay-set artifact path), and — after Phases 5–6 — the validation and Protocol A evidence with the e2e-testing.md row reference.
5. No `updated_anchor` rows exist, so no registry/discovery/code edits are in scope. If any validation failure suggests a code edit is needed, that is not license to make it: STOP `generation-plan-defect` (the replayed content — the pin bump — is incompatible with the target in a way generation missed).

**Phase 5 — post-replay validation** (§4.4 canonical set, from `pi_context_bonsai/`): `npm test`; `npm run typecheck`; `git status --short` (only the five planned targets modified/untracked). Results appended to the baseline artifact as p-rows; must be no worse than baseline, and b02/b03's post-replay counterparts must be green (gate 8). A failure here: STOP `generation-plan-defect`.

**Phase 6 — e2e gate** (§4.4 required minimum; commands copied from `docs/pi-e2e-runbook.md` — if the runbook lacks a needed command, STOP `structural-missing-command-binding`, never improvise):
1. E2E-07 automated: from `pi_context_bonsai/`, with `BONSAI_E2E_PROVIDER`/`BONSAI_E2E_MODEL` already exported (Phase 0 verified them; do not re-assign them in the command): `timeout 900 bash test/e2e/run-e2e.sh`. Exit 0 = PASS; append the Test Runs row to `docs/e2e-testing.md`, filling the `Provider/Model` column in the existing rows' `<provider> / <model>` format. The run's install log must show it installed `@mariozechner/pi-coding-agent@0.73.1` (runtime binding re-assert, §3.6).
2. E2E-01 placeholder half + E2E-03 retrieve roundtrip: runbook Parts 2–3 (manual-drive environment at `<version>` = `0.73.1`, session P1–P6; name each drive's output after its runbook step — the drive files are `P1,P2,P4,P5,P6.jsonl`/`.err`; P3 is a `jq` assertion producing no drive output). Before the runbook's teardown, copy the evidence out of the throwaway: `mkdir -p /tmp/pi-bonsai-e2e/0.73.1/e2e-01-03 && cp "$WORK"/run-cwd/P*.jsonl "$WORK"/run-cwd/P*.err "$WORK"/session/*.jsonl /tmp/pi-bonsai-e2e/0.73.1/e2e-01-03/`. Manual-drive hygiene: if `~/.pi/agent/extensions/context-bonsai` already exists at Phase 6 start (a prior run's leftover), STOP `env-workspace-dirty` rather than removing it — the operator dispositions it. Verdicts per the runbook; `BLOCKED` carries its reason code and is not a waiver — an accepted `BLOCKED` at seal needs the gate-11 reviewer+judge exception recorded per §1.7 before sealing.
3. Oracle discipline: transform effects from host state (session JSONL, event stream, model-visible placeholder quoting), never the model's self-report; e2e docs may be updated, never narrowed (§3.6).
4. Then commit the replay in `pi_context_bonsai/` — one commit with the exact staging list `git add package.json package-lock.json test/e2e/run-e2e.sh docs/binding-verification-0.73.1.md docs/e2e-testing.md` (never `git add -A`), subject `Forward-port: pin Pi 0.73.1 and certify binding sites (cycle dd3c8794)`, body citing this plan and the gate evidence. Record the commit SHA in the final report and intent log.

**Phase 7 — release-gate ordering and final verification** (§3.8, gate 14): from parent root, `git add pi_context_bonsai .agents/plans/validation/baseline-dd3c87940cb1d65ec193a78d68045ed0c6194ecc.json && git commit` (subject `Advance pi_context_bonsai pin: 0.73.1 forward-port cycle sealed`, body naming the side-repo replay commit SHA and the landed baseline artifact — the "land cycle artifacts + advance pin" precedent); then final verification — side repo: `git -C pi_context_bonsai log --oneline -3`, `git -C pi_context_bonsai diff --name-status HEAD~1..HEAD` (exactly the five targets), `git -C pi_context_bonsai status --short` (empty); parent: `git status --short` (only the enumerated ` M tweakcc_context_bonsai` remains), `git diff --submodule=short HEAD~1..HEAD`, spec immutability `test -f docs/agent-specs/forward-port-spec.md && test -z "$(git diff --name-only -- docs/agent-specs/forward-port-spec.md)"`; detector: `node scripts/detect-pending-target.mjs --harness Pi` exits 0 `up-to-date` (acceptance criterion 4). **The routine cycle ends here — nothing is pushed; this shape has no publish ladder and the executor must not invent one (§3.8). Outward actions are a separate owner-approved step.**

**Phase 9 — routine maintenance** (§1.16; runs after seal or after any STOP): maintenance report at `.agents/plans/maintenance-report-dd3c87940cb1d65ec193a78d68045ed0c6194ecc.md`, committed to the parent as its own commit (subject `Record maintenance report: pi 0.73.1 cycle`, body summarizing verdicts — the committed `maintenance-report-*.md` precedents); every stumble gets a `SPEC-GAP` vs `EXECUTOR-FAIL` verdict; any Part 4 slot-fact change is left uncommitted in the working tree with the edited path named in the final report (owner tier reviews and commits); when nothing changed, the report says so explicitly.

## 8. Validation commands by working directory

From `pi_context_bonsai/`: `npm install`; `npm test`; `npm run typecheck`; `bash test/e2e/run-e2e.sh` (env-gated, wrapped in `timeout 900`); `git status --short`.
From parent root: `git status --short`; `git -C pi_context_bonsai rev-parse HEAD`; `node scripts/detect-pending-target.mjs --harness Pi`; the spec-immutability pair; `git diff --submodule=short HEAD~1..HEAD`.
From `$WORK/run-cwd` (runbook manual drives): the `pi -p --mode json --provider ... --model ... --session-dir ...` turn shape with per-turn `timeout 120`.

## 9. E2E gate section

Procedure docs: `pi_context_bonsai/docs/e2e-protocol.md` (scenario semantics, verdict rules) and `pi_context_bonsai/docs/e2e-testing.md` (Protocol A normative method); command binding: `docs/pi-e2e-runbook.md`. Required scenario set for this cycle (immutable, §3.6): **E2E-07, E2E-03, E2E-01 (placeholder half)** — the §4.4 fixed minimum with zero `updated_anchor` additions. E2E-02/04/05/06 are carried by the integration suite inside `npm test` this cycle. The installation e2e (sprite) is not in this cycle's required set: it remains the recorded pre-parity gap in the protocol doc's Parity claim section and is unaffected by a pin bump that changes no install commands; running it stays a separate, explicitly-invoked exercise.

## 10. Worktree artifact check

Recorded in Phase 2; generation-time state: all four tracked targets clean, the new doc absent, no overlapping untracked files. Executor re-runs before its first edit.

## 11. Plan approval and commit status

- Approval status: **approved** (see §12 — §1.15 loop closed with zero blocking findings at iteration 2).
- Approval citation: §1.15 independent reviewers (missing-details; ambiguity — both repository-inspecting) + the Fable relay session as judge under the standing DRIVE delegation; local landings routine per the standing order (side-repo `HAND_OFF.md` §"Standing order", owner directive 2026-07-05) — recorded here as the Phase 7 landing authorization. Outward actions remain owner-gated.
- Plan commit hash: recorded in the committing relay's hand-off (`git log --follow` on this file resolves it); the §1.14 item-11 commit is the one that adds this file and the replay-set artifact together.
- Ready for orchestration: **yes** — pending only the launch context naming an intent-log path (§1.18) and out-of-band `BONSAI_E2E_PROVIDER`/`BONSAI_E2E_MODEL`.

## 12. Validation loop results (§1.15)

- **Rehearsal (closed-artifact form, §4.4 realization), 2026-07-05, iteration 1**: disposable scratch clone checked out at `SOURCE_HEAD_SHA` (`/tmp/pi-cal-probe`), all Phase-4 step 1–2 edits applied verbatim (four `package.json` fields, `PI_PKG`), then `npm install` (exit 0), `npm run typecheck` (exit 0), `npm test` (16 files, 97 tests, all pass). Green — no target-API adaptation needed; consistent with the drift scan's 11/12 `unchanged_anchor`. Scratch clone removed after evidence capture.
- **Iteration 1 reviews (2026-07-05, two independent repository-inspecting Opus reviewers)**: missing-details — 1 blocking (the parent baseline artifact was never committed, making Phase 7's clean-status assertion and gate 6 unsatisfiable; fixed by staging it in the Phase 7 parent commit) + notes (Provider/Model cell format, p-row ids, Phase 2 wording, `~/.pi` leftover hygiene — all folded). Ambiguity — 5 blocking (unbound `...` env placeholders inside the Phase 6 command, removed; plan/replay-set committed-before-run precondition unstated, bound in Phase 2; baseline/maintenance-report commit dispositions, bound in Phases 7/9; side-repo commit staging list unbound, bound explicitly with `git add -A` forbidden; §1.18 tier applicability left as executor judgment, frozen as a plan fact) + notes (P1–P6 evidence copy-out before teardown with named turn files, parent-commit body directive — folded; the E2E-01 placeholder-half verdict is judgment-rendered by the judge, acknowledged, not machine-checkable).
- **Iteration 2 (fresh pass on the revised plan, 2026-07-05, independent repository-inspecting Opus reviewer covering both dimensions)**: **zero blocking findings** — all seven iteration-1 fixes verified present and coherent; copy-out globs verified against the runbook's actual output shapes; the Phase 2 dirty enumeration walked clean at every phase boundary; no unbound placeholder remains in any runnable command. One traceability note (P3 is an assertion, not a drive — drive-file naming clarified in Phase 6) folded. Loop closed early per §1.15.

## 13. Completion checklist (mirrors §1.13 seal gates)

1. Every inventory item (12 registry rows) has exactly one replay-set row. ☐
2. Zero unresolved `manual_review`/`removed_or_ambiguous_anchor` rows (none exist). ☐
3. Zero unapproved late fixes. ☐
4. Replay-set artifact present and schema-valid (committed form; no checksum per Part 3). ☐
5. (No separate approval artifacts in this shape.) ☑ n/a
6. Baseline artifact complete: all rows schema-valid, no placeholders, provenance present. ☐
7. Replay verification: side-repo `git diff --name-status HEAD~1..HEAD` = exactly the five planned targets; `package.json` pins and `PI_PKG` show `0.73.1`. ☐
8. Post-replay validation no worse than baseline; must-be-green rows green. ☐
9. Change-scope check clean, or exceptions §1.7-approved before landing. ☐
10. No unresolved exception records. ☐
11. E2E gate: E2E-07 PASS + E2E-03 PASS + E2E-01 placeholder half PASS, or reviewer+judge-approved exception. ☐
12. Spec immutability commands pass. ☐
13. Reviewer and judge approvals recorded (§11). ☐
14. §3.8 ordering: side-repo commit → parent pin advance → final verification, all green; nothing pushed. ☐
