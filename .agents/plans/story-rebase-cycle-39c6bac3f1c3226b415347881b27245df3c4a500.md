# Story: Forward-port cycle — Hermes Context Bonsai onto hermes-agent v2026.7.1 (pressure test)

Routine forward-port cycle per `docs/agent-specs/forward-port-spec.md` (Part 1 core, Part 3 shape binding — git-tag pure-extension variant, §4.8 Hermes Agent slot table). Generated by the owner tier on 2026-07-07 from the spec plus the §4.8 slots; this is the Stage 6 acceptance-gate plan (`derivation-pipeline-spec.md` §9) in **`uncommitted-pressure-test` mode (§1.11)**: no Hermes release is pending (detector: `up-to-date` at 2026.7.1), so the current release is the pressure-test target and the target version equals the port's derivation version. All relative paths are from the parent repo root (`context-bonsai-agents/`); side-repo commands state their working directory.

## 1. Goal, Non-Goal, execution outcome

**Goal**: prove the §4.8 binding by driving the routine path end-to-end against the current release — freeze the target identity, re-verify every binding-site anchor against the frozen target clone, and certify the result through the slot's canonical validation set and e2e gates — stopping short of committing.

**Non-Goal**: no plugin behavior changes, no new features, no spec edits (gate 12), no new evidence docs committed (the target equals the derivation version, whose evidence is already committed), nothing pushed anywhere.

**Execution outcome**: `hermes_context_bonsai` HEAD still equals `SOURCE_HEAD_SHA` at the end (§1.11 pressure-test semantics — this shape's replay binds zero code edits for this target); verification uses working-tree commands; all applicable seal gates pass with the pressure-test realizations this plan states. Everything stays local.

## 2. Frozen inputs (§1.1)

| Input | Frozen value |
|---|---|
| `SOURCE_HEAD_SHA` (side repo `hermes_context_bonsai`) | `39c6bac3f1c3226b415347881b27245df3c4a500` |
| `SOURCE_REF` | `refs/heads/main` (side repo tracked branch) |
| Frozen target identity (§3.1, git-tag realization) | tag `v2026.7.1`, peeled commit SHA `7c1a029553d87c43ecff8a3821336bc95872213b`, from `git ls-remote --tags https://github.com/NousResearch/hermes-agent 'refs/tags/v2026.7.1*'` (captured 2026-07-07) |
| Artifact storage (§3.1 manifest realization) | `/tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/` — `clone/` at the frozen SHA, `manifest.json` (tag, peeled SHA, clone command, `uv sync` record) |
| Validation mode (§1.11) | `uncommitted-pressure-test` — no pending release; current release as pressure-test target (rationale recorded here per §1.11) |
| Supersession lineage | none — first Hermes cycle |

There is no patched install and no npm identity in this variant: the runtime under test is the frozen clone, and the runtime target binding is `git -C /tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone rev-parse HEAD` = the frozen SHA, with every drive run through `uv run --project <clone>` (§4.8 Upstream identity).

**Same-target evidence-doc fact (binds Phase 3's b04)**: the target version equals the derivation version, so the cycle's ported-version evidence doc `docs/binding-verification-2026.7.1.md` already exists at `SOURCE_HEAD_SHA` (the derivation seed). This cycle produces no new binding-verification doc; b04 asserts the seed's presence instead of §1.6's absence probe, and the deviation is recorded here, not improvised.

## 3. Allowlists, planned targets, explicit non-targets

**Planned target files: none.** The drift scan (§4 below) binds zero `updated_anchor` rows — the target SHA equals the SHA every binding was derived against — so the replay phase applies no side-repo edits. In pressure-test mode the run's writes are confined to out-of-repo evidence paths: `/tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/` and `$HERMES_BONSAI_SCRATCH_ROOT` (this run: `/tmp/hermes-bonsai-scratch-cycle`), plus the run's intent log and log directory. If any validation failure suggests a side-repo edit is needed, that is not license to make it: STOP `generation-plan-defect`.

Explicit non-targets (§4.8): `docs/agent-specs/forward-port-spec.md`; the operator's real `~/.hermes/**`; the harness clone's tree as a commit target; `opencode/`, `tweakcc_context_bonsai/`, `pi/`, `pi_context_bonsai/`, `codex/`, `codex_context_bonsai/`, `cline/`, `cline_context_bonsai/`, `kilo/`, `kilo_context_bonsai/`; prior cycles' plans; the shared `qwen25-6k` ollama model (never deleted by teardown); every file in `hermes_context_bonsai/` (no planned targets in this mode).

## 4. Classification (§1.2–§1.3, §3.2–§3.4)

The inventory is the §4.8 seam/anchor registry: the 12 binding keys of the Binding Sites table in `docs/agent-specs/hermes-agent-context-bonsai-bindings.md`. The committed replay-set artifact is the classification record (§1.14 item 4, closed-artifact form):

- **Artifact**: `.agents/plans/validation/replay-set-39c6bac3f1c3226b415347881b27245df3c4a500.json` (12 rows, sorted by `anchor_id`; §3.4 field order; committed and diff-tracked — no checksum, per §1.4's Part 3 binding).
- **Outcome**: 12 × `unchanged_anchor`. Zero `updated_anchor`, zero hard-blocking rows — no §1.5 approvals needed beyond this plan's own approval citation, which every row carries via this section.
- Drift-scan evidence: independent repository-inspecting scan, 2026-07-07, against the derivation clone at `/tmp/hermes-bonsai-derivation/hermes-agent` — identity re-verified at scan start (`git rev-parse HEAD` = the frozen SHA, so it is byte-identical to the artifact clone) — and the side repo at `SOURCE_HEAD_SHA`; per-row citations in the artifact's `rationale`/`evidence_ref` fields. The scan reported zero citation defects: every verified harness-side site and every named side-repo symbol resolved as documented.

## 5. Shape-required analysis: anchor re-derivation records (§1.14 item 5)

No anchor required re-derivation: the target commit is byte-identical to the derivation commit, and the scan verified each row's harness-side citations and side-repo realization symbols resolve as documented (all 12 rows `unchanged_anchor`). The `presentation-middleware` row remains deliberately unrealized per its Binding Sites entry; its harness substrate citations were verified like every other row's.

## 6. Acceptance criteria

1. Baseline and post-replay canonical validation green from `hermes_context_bonsai/` against the frozen clone: `pytest tests -q` (91 tests), `uvx ruff@0.15.10 check .`, `bash scripts/run-scenarios.sh` 8/8 (gate 8).
2. E2E gate: the 8-row matrix PASS **plus** the live-model Protocol A behavioral oracle PASS via `scripts/drive-protocol-a-live.sh` (gate 11; §4.8 required minimum; zero `updated_anchor` rows, so no additional drives are pulled in).
3. `node scripts/detect-pending-target.mjs --harness 'Hermes Agent'` reports `up-to-date` (exit 0) — the Release detection slot resolves through the seed doc.
4. Side repo working tree clean and HEAD unmoved at `SOURCE_HEAD_SHA` after the run (§1.11 pressure-test verification; gate 14's pressure-test realization).
5. Zero unresolved exception records; spec untouched (gates 10, 12).

## 7. Implementation phases

Every phase-ending STOP records exactly one §1.17 reason code (`escalation-reason-code: <code>` line) in the final report and the §1.16 maintenance report. **This run's executor is below the owner tier — a frozen fact of this plan, not an executor judgment — so §1.18 binds in full**: the launcher seeds an intent log with a `RUN-START` UTC timestamp and names its path in the launch context; the intent log lives outside both repos (e.g. under `/tmp/hermes-bonsai-e2e-logs/`). **If no intent-log path is named, STOP immediately** — report the missing §1.18 binding to the invoker and do not proceed. Append an intent-log line before every side-effecting action; timestamps command-sourced via `$(date -u +%FT%TZ)`; reconcile from evidence at every phase start and before any STOP.

**Phase 0 — rig preflight** (§1.12, §4.8 Credentials: none required — both rigs are zero-credential): `ollama list | grep -q qwen25-6k` — on failure, provision per the runbook: `printf 'FROM qwen2.5:7b-instruct\nPARAMETER num_ctx 6144\n' | ollama create qwen25-6k -f -`; if ollama itself is absent or the model cannot load, the live Protocol A scenario is per-scenario `BLOCKED` (`native-runtime-missing`), not a plan-wide hard fail — and an accepted `BLOCKED` at seal needs the gate-11 reviewer+judge exception recorded per §1.7.

**Phase 1 — bootstrap and frozen-identity verification** (§1.12, §1.1):
- Toolchain: `git --version`, `uv --version`, `uvx --version`, `sqlite3 --version` all succeed; else STOP `env-toolchain-missing`.
- From parent root: `git -C hermes_context_bonsai rev-parse HEAD` must equal `39c6bac3f1c3226b415347881b27245df3c4a500`. If not: STOP `input-source-drift` (§1.9 — generate a fresh plan; never reset the branch).
- Artifact clone: if `/tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone` is missing (a `/tmp` wipe), recreate it exactly per the manifest's `clone_command` and re-run `uv sync`. Then `git -C /tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone rev-parse HEAD` must print `7c1a029553d87c43ecff8a3821336bc95872213b`; else STOP `executor-artifact-integrity`.
- Hydration: `test -d /tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone/.venv || (cd /tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone && uv sync)`.
- Env binding for every side-repo command in Phases 3, 5, and 6: prefix each command inline with `HERMES_AGENT_ROOT=/tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone HERMES_BONSAI_SCRATCH_ROOT=/tmp/hermes-bonsai-scratch-cycle` — never rely on shell-session persistence between tool invocations. This matters silently: the drives *default* `HERMES_AGENT_ROOT` to the derivation-time path when unset and would exit 0 against the wrong clone.

**Phase 2 — workspace preflight** (§1.10, §3.5): work references `hermes_context_bonsai/` in place on its tracked branch; this mode plans zero edits there. Launch precondition: the §1.14 item-11 commit adding this plan and the replay-set artifact has already landed in the parent — if `git status --short` shows either file as untracked or modified, STOP `env-workspace-dirty` (the launcher must commit them first). `git -C hermes_context_bonsai status --short` must be empty. From parent root, `git status --short` must contain only the enumerated pre-existing dirty paths: ` M tweakcc_context_bonsai`, ` M opencode.json` (pilot-provider bookkeeping), and the untracked `.agents/pilot/relaunch-run6.sh`, `blog-1-context-bonsai.md`, `blog-2-maintenance.md` (owner-parked). Any other dirty path: STOP `env-workspace-dirty`. Worktree artifact check (§1.14 item 10): vacuous — zero planned target files; recorded at generation, re-asserted by the executor before Phase 3.

**Phase 3 — baseline capture** (§1.6, §4.8 Baseline rows; artifact `.agents/plans/validation/baseline-39c6bac3f1c3226b415347881b27245df3c4a500.json`, row fields `row_id, command, frozen_upstream_head_sha, frozen_source_head_sha, exit_code, result, artifact_path, provenance_ref` (`frozen_upstream_head_sha` = `7c1a029553d87c43ecff8a3821336bc95872213b`, the SHA-realization precedents' field name — Hermes's frozen identity is a tag + peeled SHA); post-replay rows replace the baseline id's `b` with `p` (`p02` mirrors `b02`, `p03` mirrors `b03` — never `pb02`), e2e rows use `e01`-style ids; every row's `result` is uniformly `PASS`/`FAIL`/`BLOCKED` (a command's echo strings such as `present-as-expected` belong in the captured log, not the `result` field); every row's `artifact_path` names its captured log `$RUN_LOG_DIR/<row_id>.log`, where `RUN_LOG_DIR` is the launch context's log directory (§1.18 — the same context that names the intent-log path); the executor creates the file as a JSON array of row objects with exactly those fields in that order — the committed `baseline-*.json` precedents are the shape reference). All side-repo commands run from `hermes_context_bonsai/` with the Phase 1 inline env prefix on every command:
- b01: the Phase 1 clone + `uv sync` result (no fresh command — its `artifact_path` is `$RUN_LOG_DIR/b01.log`, into which Phase 1's clone-identity and `uv sync` output were captured; the §1.12 hydration row)
- b02 (must-be-green): `uv run --project "$HERMES_AGENT_ROOT" pytest tests -q`
- b03 (must-be-green): `uvx ruff@0.15.10 check .`
- b04: `test -f docs/binding-verification-2026.7.1.md && echo present-as-expected || (echo missing-seed && false)` — the §2 same-target fact inverts §1.6's absence probe for this cycle only; `missing-seed` after Phase 1's HEAD check passed means the working tree deviates from its own HEAD content: STOP `env-workspace-dirty`.
- A must-be-green failure at baseline: STOP `generation-plan-defect`; do not fix breakage in-cycle.

**Phase 4 — replay** (§3.5): no rows bind edits (§4). The replay step for this cycle is the null edit set; proceed to validation. Any impulse to edit is a STOP per §3.

**Phase 5 — post-replay validation** (§4.8 canonical set, from `hermes_context_bonsai/`, Phase 1 env): `uv run --project "$HERMES_AGENT_ROOT" pytest tests -q`; `uvx ruff@0.15.10 check .`; `bash scripts/run-scenarios.sh` (exit 0 only on 8/8 — the pinned-target artifact-evidence realization); `git status --short` (must stay empty). Results appended as p-rows; must be no worse than baseline; a failure here: STOP `generation-plan-defect`.

**Phase 6 — e2e gate** (§4.8 required minimum; commands copied from `docs/hermes-e2e-runbook.md` — if the runbook lacks a needed command, STOP `structural-missing-command-binding`, never improvise):
1. The 8-row matrix verdict carries over from Phase 5's `run-scenarios.sh` row (rows map to engine liveness, E2E-01/02/03/04/05/06 and E2E-07's payload half per the procedure doc's coverage table).
2. E2E-07 behavioral half, live model: `HERMES_AGENT_ROOT="$HERMES_AGENT_ROOT" bash scripts/drive-protocol-a-live.sh` from `hermes_context_bonsai/` (the runbook's zero-credential rig; the drive manages its own proxy and scratch sessions). Exit 0 = PASS; record the e-row with the drive's printed assertion results. BLOCKED per Phase 0's rule.
3. Oracle discipline: verdicts from host state (`state.db` active rows, the archive JSON, persisted final answer), never the model's self-report; e2e docs may be updated, never narrowed (§3.6) — though this mode plans no doc edits; a Run records row for this pressure test is NOT appended (nothing is committed in this mode; the §1.16 maintenance report is the run's durable record).

**Phase 7 — final verification** (§1.11 pressure-test realization of §3.8/gate 14): from parent root — `git -C hermes_context_bonsai rev-parse HEAD` still `39c6bac3f1c3226b415347881b27245df3c4a500`; `git -C hermes_context_bonsai status --short` empty; `git status --short` shows only the Phase 2 enumeration plus the untracked baseline artifact (committed by the invoker with the run's closing bookkeeping, per the §1.20-invoker convention — the executor never commits); spec immutability `test -z "$(git diff --name-only -- docs/agent-specs/forward-port-spec.md)"`; detector `node scripts/detect-pending-target.mjs --harness 'Hermes Agent'` exits 0 `up-to-date`. STOP codes for Phase 7 failures: spec-immutability failure → `executor-artifact-integrity` (the §1.17 registry binds gate 12's assert to that code); side-repo HEAD moved or side-repo dirty → `executor-scope-violation` (the null edit set means only the run itself can have landed content there); parent status outside the Phase 2 enumeration plus this run's own recorded artifacts → `env-workspace-dirty`; detector exit 3 (`pending-target` — an upstream release published mid-run) → `input-source-identity-stale`; any other detector non-zero exit → `generation-plan-defect`. **Nothing is committed to the side repo and nothing is pushed; this shape has no publish ladder and the executor must not invent one.**

**Phase 8 — routine maintenance and final report** (§1.16; runs after seal or after any STOP): one document at `.agents/plans/maintenance-report-39c6bac3f1c3226b415347881b27245df3c4a500.md` carrying both the run's final report (phase outcomes, gate results, any STOP with its reason code) and the §1.16 maintenance record (left for the invoker to commit with the baseline artifact) — this path is the plan's binding for every earlier "final report" reference; every stumble gets a `SPEC-GAP` vs `EXECUTOR-FAIL` verdict; any Part 4 slot-fact change is left uncommitted with the edited path named in the report; when nothing changed, the report says so explicitly.

**Phase 9 — scratch-state removal (§1.19; runs last, only after Phase 8's document exists on disk)**: `rm -rf "$HERMES_BONSAI_SCRATCH_ROOT"` and `rm -rf /tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1` (a STOPped run preserves both until dispositioned — Phase 8's evidence duties come first by construction). The intent log and run log directory are the invoker's to sweep after review.

## 8. Validation commands by working directory

From `hermes_context_bonsai/` (with `HERMES_AGENT_ROOT` and `HERMES_BONSAI_SCRATCH_ROOT` exported per Phase 1): `uv run --project "$HERMES_AGENT_ROOT" pytest tests -q`; `uvx ruff@0.15.10 check .`; `bash scripts/run-scenarios.sh`; `bash scripts/drive-protocol-a-live.sh`; `git status --short`.
From parent root: `git status --short`; `git -C hermes_context_bonsai rev-parse HEAD`; `node scripts/detect-pending-target.mjs --harness 'Hermes Agent'`; the spec-immutability check.
From `/tmp/hermes-bonsai-artifacts/hermes-agent/2026.7.1/clone`: `git rev-parse HEAD`; `uv sync` (bootstrap only).

## 9. E2E gate section

Procedure doc: `hermes_context_bonsai/docs/e2e-testing.md` (all eight per-harness slots; scenario semantics, verdict rules, pattern-dictation discipline); command binding: `docs/hermes-e2e-runbook.md`. Required scenario set for this cycle (immutable, §3.6): **the full 8-row matrix plus E2E-07's live-model behavioral half (Protocol A)** — the §4.8 fixed minimum with zero `updated_anchor` additions. The installation e2e is not in this cycle's required set: the local variant is EXECUTED derivation evidence, the sprite post-publish variant is recorded as pending the publish cycle in the procedure doc's slot 7, and a pressure test that changes no install commands does not move either fact.

## 10. Worktree artifact check

Vacuous — zero planned target files (§3). Generation-time state recorded: `hermes_context_bonsai` clean at `SOURCE_HEAD_SHA`; the baseline artifact path and maintenance-report path do not exist. Executor re-asserts before Phase 3.

## 11. Plan approval and commit status

- Approval status: **approved** (see §12 — §1.15 loop closed with zero blocking findings at iteration 2).
- Approval citation: §1.15 independent reviewers (missing-details; ambiguity — both repository-inspecting) + the Fable relay session as judge under the standing DRIVE delegation; local landings routine per the standing order (side-repo `HAND_OFF.md` §"Standing order", owner directive 2026-07-05). Outward actions remain owner-gated.
- Plan commit hash: recorded in the committing relay's hand-off (`git log --follow` on this file resolves it); the §1.14 item-11 commit is the one that adds this file and the replay-set artifact together.
- Ready for orchestration: **yes** — pending only the launch context naming an intent-log path (§1.18). No credentials are required (§4.8).

## 12. Validation loop results (§1.15)

- **Bindings-freshness consultation (2026-07-07)**: latest pass record `docs/agent-specs/bindings-reverification-2026-07-06.md` predates the Hermes bindings document's own derivation (Stages 1–5, sealed 2026-07-07) and probes six other harnesses — no Hermes rows exist in any pass record. Check 1: zero DEMOTED rows and zero in-place `**UNVERIFIED**` annotations in `hermes-agent-context-bonsai-bindings.md` (grep-verified). Check 2: the pass record's date (2026-07-06) is earlier than the harness's `last_cycle_start` (2026-07-07T09:38:22Z, the binding-act seed) — recorded as the stale-evidence flag, with the mitigating fact that the bindings document itself is one day fresher than the pass record and was source-truth-reviewed at Stage 5. Check 3: n/a (a record exists).
- **Rehearsal (closed-artifact form, §4.8 realization), 2026-07-07, iteration 1**: disposable scratch clone of the side repo at `SOURCE_HEAD_SHA` (`/tmp/hermes-cal-rehearsal`) + freshly provisioned artifact clone per the §4.8 Artifact storage slot (clone command executed end-to-end, converting the runbook's COMPOSED provision to exercised reality; peeled-SHA assertion passed). Zero `updated_anchor` edits to apply (the null replay). Canonical set: `pytest tests -q` 91/91, `uvx ruff@0.15.10 check .` clean, `bash scripts/run-scenarios.sh` **ALL ROWS PASS (8/8)** against the fresh clone. Scratch clone and rehearsal scratch home removed after evidence capture; the artifact clone is retained as this cycle's frozen runtime under test.
- **Iteration 1 reviews (2026-07-07, two independent repository-inspecting Sonnet reviewers)**: missing-details — zero blocking (all repo-state claims, script paths, clone identity, detector behavior, ollama model presence, and the replay set's 12-row/12-key correspondence verified live; one note folded: baseline field renamed `frozen_upstream_head_sha` to match the SHA-realization precedents; one cosmetic note acknowledged: runbook invokes the Protocol A drive parent-root-relative while the plan invokes it from the side repo — functionally identical, the script self-locates via `BASH_SOURCE`). Ambiguity — 6 blocking, all fixed: inline env prefix bound on every command (the drives' silent wrong-clone default); baseline `result` vocabulary bound `PASS`/`FAIL`/`BLOCKED` with `artifact_path` = `$RUN_LOG_DIR/<row_id>.log` (launch-context log directory); p-row ids bound as `b`→`p` replacement; the final report bound to the Phase 8 maintenance-report document; Phase 7 failures given explicit STOP codes; Phase 8/9 reordered so scratch removal strictly follows the report's existence. Plus one note folded: b04's `missing-seed` STOP recoded `env-workspace-dirty` (post-HEAD-check, only tree mutilation can produce it).
- **Iteration 2 (fresh pass on the revised plan, 2026-07-07, independent repository-inspecting Sonnet reviewer covering both dimensions)**: all seven iteration-1 fixes verified present and coherent; one blocking finding — Phase 7 routed the spec-immutability failure to `executor-scope-violation` while the §1.17 registry binds gate 12's assert to `executor-artifact-integrity`; fixed. One note folded: b01's `artifact_path` bound explicitly to `$RUN_LOG_DIR/b01.log`.
- **Iteration 3 (targeted re-check of the two iteration-2 edits, 2026-07-07, independent repository-inspecting Sonnet reviewer)**: **zero blocking findings, zero notes** — the STOP mapping now mirrors the registry verbatim with no conflicting mapping elsewhere in the plan; the b01 binding is coherent with Phases 1 and 3. Loop closed per §1.15.

## 13. Completion checklist (mirrors §1.13 seal gates)

1. Every inventory item (12 registry rows) has exactly one replay-set row. ☐
2. Zero unresolved `manual_review`/`removed_or_ambiguous_anchor` rows (none exist). ☐
3. Zero unapproved late fixes. ☐
4. Replay-set artifact present and schema-valid (committed form; no checksum per Part 3). ☐
5. (No separate approval artifacts in this shape.) ☑ n/a
6. Baseline artifact complete: all rows schema-valid, no placeholders, provenance present. ☐
7. Replay verification (pressure-test realization): side-repo working tree clean, HEAD unmoved at `SOURCE_HEAD_SHA`. ☐
8. Post-replay validation no worse than baseline; must-be-green rows green. ☐
9. Change-scope check clean (null edit set), or exceptions §1.7-approved. ☐
10. No unresolved exception records. ☐
11. E2E gate: 8/8 matrix PASS + Protocol A live PASS, or reviewer+judge-approved exception. ☐
12. Spec immutability commands pass. ☐
13. Reviewer and judge approvals recorded (§11). ☐
14. Pressure-test close: nothing committed to the side repo, nothing pushed; final verification green. ☐
