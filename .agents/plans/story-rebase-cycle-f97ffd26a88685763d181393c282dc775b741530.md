# Story: Forward-port cycle — Cline Context Bonsai onto upstream release `v2.17.0-cli` (Stage 6 acceptance plan)

Routine forward-port cycle per `docs/agent-specs/forward-port-spec.md` (Part 1 core, Part 2 shape binding — git-fork, §4.6 Cline slot table). Generated by the owner tier on 2026-07-06 from the spec plus the §4.6 slots. This is the **first routine cycle plan for the Cline harness** and the Stage 6 acceptance-gate plan (`derivation-pipeline-spec.md` §9): the §4.6 binding is proven by the routine path consuming it. Unlike the Codex Stage 6 precedent, a real upstream release is pending, so this is a `committed-final` plan against `v2.17.0-cli`; the acceptance gate is the §1.15 generation validation loop (including the target-resolution rehearsal), and execution remains a separate, later act by the routine path. All relative paths are from the parent repo root (`context-bonsai-agents/`); worktree/side-repo commands state their working directory.

## Goal

Replay the Cline Context Bonsai port — the contiguous 12-commit `[CB-cline.1]`/`[Story C1]`–`[Story C3]` suffix currently tipped at `SOURCE_HEAD_SHA=f97ffd26a88685763d181393c282dc775b741530` on `feat/spec-compliance` — onto the frozen upstream release tag `v2.17.0-cli` in an isolated worktree, pass the canonical validation set and the live e2e gate (including the port's first live E2E-04), tag the rebase point, and advance the parent pin locally. Pushes (§2.9 steps 5–6) are owner-gated and excluded from this plan's executable scope.

## Non-Goal

- No code changes to `cline_context_bonsai/` (no replay-set row binds any; only the e2e results doc and Run records row may land there per §4.6 Evidence paths).
- No replay/rewrite of the source branch in place; all replay happens in the scratch worktree.
- No targeting of `v2.18.0-cli` or `v3.32.6-cli`: the invoker-selected target is `v2.17.0-cli` (see Frozen Inputs rationale). Later releases are later cycles.
- No pushes: §2.9 steps 5–6 (publish, `main` fast-forward) execute only as their own owner-approved continuation, per §2.9's allowance ("do not push; steps gated on reviewer+judge approval"). The cycle is not complete until that ladder completes.
- Generation does not create the worktree, build, or commit anything in the fork; the executor performs those on execution. (The §1.15 rehearsal used its own disposable worktree, removed at loop close.)

## Execution Outcome Statement

After execution, the worktree's `HEAD` is based on `UPSTREAM_HEAD_SHA` (`07593bb42a`, = `v2.17.0-cli`) and contains exactly the 12 replayed commits from the approved replay set in topological order — verified by cherry-pick provenance trailers, never by source-SHA equality. The canonical validation set and the required live e2e scenario set are green; the local tag `bonsai/v1-on-cline-2.17.0` exists at the replayed tip; the parent pin is advanced on `pin-advance/cline-2.17.0`; nothing is pushed.

## Frozen Inputs (§1.1)

Invoker-supplied, frozen at cycle start; never re-resolved. All commands reference the frozen values below.

- `SOURCE_REF`: `refs/heads/feat/spec-compliance`
- `SOURCE_HEAD_SHA`: `f97ffd26a88685763d181393c282dc775b741530`
- `UPSTREAM_REF`: `refs/tags/v2.17.0-cli` (fetched per §2.1's explicit tag fetch: `git fetch upstream "refs/tags/v2.17.0-cli:refs/tags/v2.17.0-cli"` — the upstream remote's refspec is heads-only)
- `UPSTREAM_HEAD_SHA`: `07593bb42ae95793c6dbf39918c8205f0fdf07bb` (`git rev-parse --verify refs/tags/v2.17.0-cli^{commit}`)
- **Target-selection rationale**: at generation, three stable `-cli` releases are pending past the port's `v2.16.0-cli` anchor (`v2.17.0-cli`, `v2.18.0-cli`, `v3.32.6-cli`; `scripts/detect-pending-target.mjs --harness Cline` reports the newest, 3.32.6). The detector's signal is a detection, not target selection (§"How a routine cycle uses this spec"); the invoker selects `v2.17.0-cli` — the smallest pending release — for the harness's first routine cycle, minimizing replay drift. This is an invoker choice recorded here, not a spec default.
- `BASE_SHA`: `70f0e8d548329f72e4c70378c377847d1f5ce2ed` — **computed, not asserted** (§2.1): `git merge-base 07593bb42a f97ffd26a8`. The port suffix sits on upstream `main` at this commit; `v2.17.0-cli` contains it, so the inventory range `UPSTREAM_HEAD_SHA..SOURCE_HEAD_SHA` holds exactly the 12 port commits and no upstream history.
- `TARGET_WORKTREE`: `/home/basil/projects/context-bonsai-agents/cline/.agent_tmp/rebase-on-v2.17.0-cli`
  - **Sibling-path shim** (§4.6 Naming): before any post-replay hydration in the worktree, ensure `cline/.agent_tmp/cline_context_bonsai` → `../../cline_context_bonsai` exists — the replayed `file:` dependency (`"cline-context-bonsai": "file:../cline_context_bonsai"`) resolves one directory up from the fork root, which from the worktree lands in `.agent_tmp/`. Step-scoped scratch, removed with the worktree per §1.19. Clean-upstream baseline rows need no shim (upstream has no such dependency).
- `TARGET_BRANCH`: `replay/context-bonsai-on-cline-2.17.0`
- `TARGET_TAG_AT_TIP`: `bonsai/v1-on-cline-2.17.0` (created locally at §2.9 step 1; **not pushed** in this plan's scope)
- `PARENT_PIN_ADVANCE_BRANCH`: `pin-advance/cline-2.17.0` (§2.9 step 3; local only)
- `VALIDATION_MODE`: `committed-final` (§1.11 default; a real release is the target)
- Range scope: `UPSTREAM_HEAD_SHA..SOURCE_HEAD_SHA` (default; no author-filter override)
- Upstream commits in range: 0 (the range is exactly the 12 port commits; `git cherry` marks all 12 `+`)
- Supersession lineage: none (first Cline cycle; no prior plan superseded)

## Allowlists and Planned Targets (§1.14 item 3)

Per §4.6, no overrides for this cycle:

- **Runtime allowlist**: fork paths `src/**`, `cli/**`, `proto/**`, `package.json`, `package-lock.json`; side-repo paths `cline_context_bonsai/{src/**,test/**,package.json,package-lock.json}` (no row binds any this cycle).
- **Docs allowlist**: `.agents/plans/**`, `cline_context_bonsai/docs/**`.
- **State-only**: metadata/state artifacts outside both.
- **No fork-owned wholesale files** (§4.6): the port leaves the fork's `README.md` untouched.
- **Planned worktree targets (replay only, not direct edits)**: the union of replay-set `target_paths` — 113 paths, all inside the runtime allowlist (`src/**` plus root `package.json`/`package-lock.json` in row `fcd05e76b8`).
- **Planned parent-repo targets (generator writes)**: this plan file; `.agents/plans/validation/replay-set-f97ffd26a88685763d181393c282dc775b741530.json`; the rehearsal evidence logs `.agents/plans/validation/rehearsal-cline-f97ffd26a.log` and `.agents/plans/validation/rehearsal-cline-siderepo.log`. **Executor writes (parent repo)**: `.agents/plans/validation/baseline-f97ffd26a88685763d181393c282dc775b741530.json`; `.agents/plans/validation/exceptions-f97ffd26a88685763d181393c282dc775b741530.json` (empty by default). **Executor writes (side repo, e2e gate)**: `cline_context_bonsai/docs/e2e-results-<DATE>-live.md` + the Run records row in `cline_context_bonsai/docs/e2e-testing.md` (§4.6 Evidence paths).
- **Explicit non-targets** (§4.6): the spec (`docs/agent-specs/forward-port-spec.md`); the source branch `feat/spec-compliance` in place; `opencode/`, `codex/`, `codex_context_bonsai/`, `pi/`, `pi_context_bonsai/`, `tweakcc_context_bonsai/`; `~/.cline/**` live user state and any `claude` CLI auth; prior cycles' worktrees; `cline_context_bonsai/` code paths.

## Source Commit Classification (§1.14 item 4)

Inventory produced by the §2.2 command sequence (run read-only at generation against `cline/`, range `07593bb42a..f97ffd26a8`): 12 commits, topological order, **zero merges** (`git log --merges` empty), all 12 marked `+` by `git cherry -v` (none patch-equivalent in upstream). Bucket counts: `required_runtime` 12; every other bucket 0. No hard-blocking rows; the >50% `manual_review` structural threshold (§1.17) is not approached.

Every row: `bucket=required_runtime`, `replay_action=cherry-pick`, `mapping_type=1:1`, `approver`=this plan's approval citation (no per-row hard-blocking approval needed). Full per-row `target_paths` (113 paths total, including the system-prompt snapshot files) are in the committed replay-set artifact; the table below summarizes.

| source_sha | subject | target_paths (summary) |
|---|---|---|
| `fcd05e76b8ce69760550779e806a14c155162e64` | [CB-cline.1] canonical prune/retrieve integration via ContextBonsaiApplier | `package.json`, `package-lock.json` (the `file:` dependency), applier + handlers + storage + tool registry under `src/core/**`, `src/shared/tools.ts` |
| `4742ff6eb0933009a2448135e1f8d66106ae08a8` | [CB-cline.1] Surface context-bonsai tools in Generic variant | generic variant config + 4 snapshots |
| `fed96916a9a268bea8d4e8b120fb2138dd47dfec` | [CB-cline.1] Reset context-history updates on bonsai mutations | `ContextManager.ts`, applier + tests |
| `99f383d9bc6dfb94001654dd73192d88a0690d8c` | [CB-cline.1] Plumb per-turn step counter into same-step retrieve guard | prune/retrieve handlers |
| `d62419c6a224353162dd19edf84bd546458ddf11` | [CB-cline.1] Gate checkpoint restore on tasks with bonsai archives | `src/integrations/checkpoints/index.ts` |
| `f345f39e5d94e36025d91ae811ab09dd08a5d089` | [CB-cline.1] Remove dead idempotent-splice guard in ContextBonsaiApplier.retrieve | applier |
| `4c605373c1794b1ee1bbde6a3b79ee04c6899d0c` | [CB-cline.1] Register context-bonsai tools in all production variants | 11 variant configs + 20 snapshots |
| `9525b2b161f3aca4175cd99052b5575fbefbfe46` | [CB-cline.1] Inject bonsai system-prompt guidance on the internal path | `bonsai_guidance.ts`, `PromptBuilder.ts` + 49 snapshots |
| `0fa435d40218cc718428c92a9c74893724431179` | [CB-cline.1] Wire in-band Context Bonsai gauge on default 5-turn cadence | `src/core/task/index.ts` |
| `e0af778bd9e04ada4731686a7e1681dcb678964b` | [Story C1] Include tool_use input in extractMessageText search corpus | applier + tests |
| `72e3f19cf197a9812d8a147ca4cab27cd15f688f` | [Story C2] Render non-text content blocks in extractMessageText | applier + tests |
| `f97ffd26a88685763d181393c282dc775b741530` | [Story C3] Wire prune-wrapper filter into ContextBonsaiApplier resolvePattern callsites | applier |

Merge-commit scan: `git log --merges 07593bb42a..f97ffd26a8` empty; no merge rows.

## Reviewer-Simplicity Evaluation (§1.14 item 5 / §2.5)

All 12 rows are `cherry-pick`. The target tag sits 3 commits past the port's base on the same upstream `main` line (base `70f0e8d5` → `v2.17.0-cli` adds a protobufjs pin, a README docs change, and the changelog tag commit), so drift against the port's touched paths is minimal; the §1.15 rehearsal (below) verifies the actual conflict/compile behavior. For each row `cherry-pick` is preferred over `cherry-pick + minor fixups` and `re-implement`. The §2.5 re-implement escape is available only if a cherry-pick conflicts irresolvably inside the row's `target_paths` — then STOP, record the reviewer-simplicity evaluation and behavioral-contract table, and obtain reviewer+judge approval before switching.

## Re-Implementation Behavioral Contract (§2.5)

Not applicable (all rows `cherry-pick`). If a row is forced to `re-implement`, populate this section with the §2.5 fields before execution proceeds.

## Validation Mode (§1.11)

Mode: `committed-final`. Replay commits land on `TARGET_BRANCH` in the isolated worktree; final verification uses commit-range commands against `07593bb42a..HEAD`; the §2.9 ladder runs through step 4 (tag, e2e gate, local pin advance, pre-publish install gate), stopping before the push steps per Non-Goals.

## Acceptance Criteria (§1.14 item 6)

- [ ] All replay work happens only in `TARGET_WORKTREE`; the executor never edits `cline/` on `feat/spec-compliance` in place and never touches any explicit non-target.
- [ ] `TARGET_BRANCH` is created from `UPSTREAM_HEAD_SHA` (`07593bb42a`) exactly; `git merge-base 07593bb42a HEAD` returns `07593bb42a`.
- [ ] Replayed inventory in `07593bb42a..HEAD` is exactly 12 commits in topological order matching the replay-set rows, identified by cherry-pick provenance trailers (or stable patch-id equivalence where conflict edits were needed). Source-SHA equality is forbidden as evidence (§2.6).
- [ ] Replay-set artifact present, schema-valid (§2.4 field order), 12 rows; checksum (below) verified before replay and unchanged at seal.
- [ ] Baseline artifact created before any replay commit; all rows carry non-null `provenance_ref`; must-be-green rows green; `r02` existence probe = `missing-as-expected`.
- [ ] Exception ledger exists (empty or with approved rows); no unresolved exceptions at seal.
- [ ] Zero `manual_review` rows (classification: all 12 `required_runtime`).
- [ ] Post-replay canonical validation set green; no net-new failures vs baseline; must-be-green rows green.
- [ ] Change-scope: worktree `git diff --name-status 07593bb42a..HEAD` lists only paths in the union of replay-set `target_paths`; generated-artifact exclusion probe returns 0 (§2.7). (`package-lock.json` rides row `fcd05e76b8` as committed port content; it is not a §2.7 generated-artifact class.)
- [ ] E2E release gate (§1.13 gate 11): the §4.6 required scenario set green against the freshly built worktree CLI — live registration, E2E-01, E2E-02, E2E-03, E2E-06 (CLI half), E2E-07/Protocol A, **and the first live E2E-04 (gauge)** — driven per `docs/cline-e2e-runbook.md`; results doc committed in the side repo.
- [ ] §2.9 steps 1–4 completed: local tag `bonsai/v1-on-cline-2.17.0`; e2e behavioral gate; local parent pin advance on `pin-advance/cline-2.17.0`; pre-publish install gate PASS (bundle-sourced per the runbook). Steps 5–6 not executed (owner-gated).
- [ ] Spec-immutability (§1.13 gate 12): `test -f docs/agent-specs/forward-port-spec.md && test -z "$(git diff --name-only -- docs/agent-specs/forward-port-spec.md)"` — or, if the spec is invoker-enumerated dirty at execution time, the §1.10 hash re-assertion form.
- [ ] Reviewer+judge approvals recorded (Approval section; standing-order routine landing).
- [ ] §1.19 cleanup rows executed on the run's realized path (seal or STOP).

## Implementation Phases (§1.14 item 7)

Working-directory shorthands: `PARENT` = `/home/basil/projects/context-bonsai-agents`; `CLINE` = `$PARENT/cline`; `WORKTREE` = `$PARENT/cline/.agent_tmp/rebase-on-v2.17.0-cli`; `SIDE` = `$PARENT/cline_context_bonsai`.

### Phase 0: Credentials preflight (§4.2 / §4.6 Credentials)

Live e2e is in scope, so credentials must be provisioned before the e2e gate (Phase 6), out of band: `cline auth -p <provider> -k <key> -m <model> --config <config-dir>` — all three flags required or the CLI hangs interactively (runbook trap). Fixed default provider on this rig: `claude-code` with sentinel key `not-a-real-key` (runbook Credentials). A missing provider at Phase 6 records the affected scenarios `BLOCKED` (`input-credentials-missing` if the seal cannot pass) — never a plan-wide preflight hard-fail.

### Phase 1: Preflight — toolchain, clean state, frozen identities (§1.10, §1.12, §2.1, §4.6)

1. Toolchain presence (§4.6): `node --version` (≥ 20); `npm --version`; `command -v jq`; `command -v sha256sum`. Script existence preflight, from `CLINE`: `jq -r '.scripts["cli:build"], .scripts["check-types"], .scripts["test:unit"]' package.json` — all non-empty.
2. Clean-state check (§1.10). From `PARENT`: `git status --short` must contain **only** invoker-enumerated pre-existing dirty paths. Enumerated for this cycle, exactly: submodule-pin lines of the form ` M <submodule>` (` M codex_context_bonsai`, ` M tweakcc_context_bonsai`-class pins — a fixed, checkable pattern: mode ` M` on a path that `git submodule status` lists), which are routinely dirty from local-only bookkeeping. **No other path is enumerated**: the Stage 6 generation-time edits (`docs/agent-specs/*.md`, `scripts/detect-pending-target.*`, `docs/cline-e2e-runbook.md`, this plan, the replay set, and the rehearsal logs) are committed before execution starts — plan approval item "Plan Commit Hash" is a precondition of execution, and those files land in the same commit series — so at execution preflight they are clean, not dirty. Any dirty path that is not a submodule-pin line fails preflight (`env-workspace-dirty`); record the observed pin set in the intent log. Also record `sha256sum docs/agent-specs/forward-port-spec.md` here and re-assert the identical value at seal gate 12 (harmless when the diff-empty form also holds; required if the spec is ever legitimately enumerated dirty).
3. Upstream tag freeze (§2.1): from `CLINE`, `git fetch --all --prune`; `git fetch upstream "refs/tags/v2.17.0-cli:refs/tags/v2.17.0-cli"`; `test "$(git rev-parse --verify refs/tags/v2.17.0-cli^{commit})" = "07593bb42ae95793c6dbf39918c8205f0fdf07bb"`.
4. Frozen-identity verification: `git rev-parse --verify refs/heads/feat/spec-compliance` → `f97ffd26a88685763d181393c282dc775b741530` (differing → `input-source-drift`, fresh plan per §1.9; never reset the branch). `BASE_SHA` recompute: `git merge-base 07593bb42ae95793c6dbf39918c8205f0fdf07bb f97ffd26a88685763d181393c282dc775b741530` → `70f0e8d548329f72e4c70378c377847d1f5ce2ed`.
5. Replay-set checksum verify (§1.4). From `PARENT`:
   `test "$(jq -c < .agents/plans/validation/replay-set-f97ffd26a88685763d181393c282dc775b741530.json | sha256sum | cut -d' ' -f1)" = "0d5692204c3c66ac3218977530b2dc25ba8b535ca59822925a851607ec08e2eb"`
6. Create the empty exception ledger if absent: `.agents/plans/validation/exceptions-f97ffd26a88685763d181393c282dc775b741530.json` = `[]`.

### Phase 2: Isolated worktree creation + sibling-path shim (§2.5, §4.6 Naming)

7. From `CLINE`: `git worktree add -B replay/context-bonsai-on-cline-2.17.0 /home/basil/projects/context-bonsai-agents/cline/.agent_tmp/rebase-on-v2.17.0-cli 07593bb42ae95793c6dbf39918c8205f0fdf07bb`
8. In `WORKTREE`: `test "$(git rev-parse HEAD)" = "07593bb42ae95793c6dbf39918c8205f0fdf07bb"`.
9. **Shim** (needed from post-replay hydration onward): `ln -sfn ../../cline_context_bonsai /home/basil/projects/context-bonsai-agents/cline/.agent_tmp/cline_context_bonsai`; verify `readlink -f` resolves to `SIDE`. If the path already exists as the identical symlink (e.g. left by the generation rehearsal), verify and reuse — not a collision.

### Phase 3: Baseline capture (clean upstream tag; no replay yet) (§1.6, §2.8, §4.6)

10. Hydrate the clean-upstream worktree (§1.12 bootstrap): from `WORKTREE`, `test -d node_modules || npm install`; `test -d cli/node_modules || (cd cli && npm install)`. (No shim needed — upstream has no `file:` dependency.)
11. Create `.agents/plans/validation/baseline-f97ffd26a88685763d181393c282dc775b741530.json`. Row schema (field order §1.6): `row_id`, `command`, `frozen_upstream_head_sha`, `frozen_source_head_sha`, `exit_code`, `result`, `artifact_path`, `provenance_ref`. Missing required fields hard-fail; `BLOCKED` only for genuinely unavailable external dependencies; no `n/a` placeholders. Rows (§4.6 baseline set):
    - `r01` (**must-be-green**), from `WORKTREE` root: `npm run check-types`.
    - `r02`, from `WORKTREE` root: `test ! -f src/core/task/ContextBonsaiApplier.ts && echo missing-as-expected || (echo unexpected-presence && false)`. `provenance_ref`: `git ls-files src/core/task/ContextBonsaiApplier.ts` (empty on the tag).
    - `r03` (**must-be-green**), from `WORKTREE` root: `npm run test:unit`.
    - `r04` (**must-be-green**), from `WORKTREE` root: `npm run cli:build` then `node cli/dist/cli.mjs version` (must report the CLI version).
    - `r05` (**must-be-green**), from `SIDE` (the real side repo — not part of the worktree): `npm test`; `r06` (**must-be-green**): `npm run typecheck`.
12. Must-be-green failure on the clean upstream tag → STOP + escalate (`env-upstream-regression`; a pre-existing upstream regression is not this cycle's to fix).

### Phase 4: Replay — cherry-pick in topological order (§2.5)

Conflict scope: each row's conflict-resolution edits must stay inside that row's `target_paths`. An edit outside that scope is an out-of-scope fixup → STOP, add an exceptions-ledger row citing the authorizing replay-set row + reviewer/judge approval refs, then continue.

13. Cherry-pick the 12 rows **in replay-set order**, `git cherry-pick -x <source_sha>`, in order: `fcd05e76b8`, `4742ff6eb0`, `fed96916a9`, `99f383d9bc`, `d62419c6a2`, `f345f39e5d`, `4c605373c1`, `9525b2b161`, `0fa435d402`, `e0af778bd9`, `72e3f19cf1`, `f97ffd26a8`.
14. Provenance-trailer count (§2.6; run identically here and at seal), from `WORKTREE`:
    `test "$(git log --format='%b' 07593bb42ae95793c6dbf39918c8205f0fdf07bb..HEAD | grep -cE '^\(cherry picked from commit [0-9a-f]{40}\)$')" = "12"`

### Phase 5: Post-replay canonical validation set (§4.6)

15. Ensure the shim exists (Phase 2 step 9), then from `WORKTREE` root: `npm install` (rehydrate with the replayed `file:` dependency), `npm run check-types` (note: the fork's script runs proto codegen and typechecks `src`, `webview-ui`, and `cli` — broader and noisier than a bare tsc; its exit code is the row result), `npm run test:unit`, `npm run cli:build`, and the offline registration probe `grep -c "context-bonsai-prune" cli/dist/cli.mjs` (non-zero; build-level only — runtime registration is Phase 6's live check). From `SIDE`: re-run `npm test`, `npm run typecheck`.
16. Baseline-delta gate: post-replay must-be-green rows green; net-new failures vs baseline are hard-fail regressions.
17. Change-scope + generated-artifact checks, from `WORKTREE`:
    - `git diff --name-status 07593bb42ae95793c6dbf39918c8205f0fdf07bb..HEAD` — every path in the union of replay-set `target_paths`; an out-of-scope landing without a prior exception → STOP + revert (`executor-scope-violation`).
    - `git diff --name-status 07593bb42a..HEAD | grep -E '(\.d\.ts$|/generated/|/__generated__/|openapi|\.gen\.|/dist/)' | wc -l` → `0` (§2.7).

### Phase 6: E2E release gate — live, required (§1.13 gate 11, §4.6, `docs/cline-e2e-runbook.md`)

Commands are **copied from `docs/cline-e2e-runbook.md`** (no invention; a missing command is a `structural-missing-command-binding` flag, not a license to improvise). Timeout wrap `timeout 240` on every LLM-invoking command; exit 124 → scenario `BLOCKED`, not `FAIL`. Evidence to `/tmp/cline-bonsai-e2e/evidence/*.txt`; config/work dirs per the runbook's fixed defaults.

Required scenario set (immutable per cycle, §4.6): live tool registration; E2E-01; E2E-02; E2E-03; E2E-06 (CLI half); E2E-07/Protocol A; **E2E-04 (gauge) — the port's first live run of it, not skippable**. Verdict semantics, explicitly: a scenario is `BLOCKED` only on the recorded reason codes (timeout exit 124, missing credentials, environmental preconditions) — and a **genuine FAIL of any required scenario is a cycle STOP**: no retries beyond the procedure doc's own drive rules, no workarounds, no narrowing; record `generation-plan-defect` per §1.17 (or `structural-protocol-a-regression` if Protocol A fails on the clean build after all prior gates passed) and escalate. Phase 0's "never a plan-wide preflight hard-fail" language covers credential *absence* only, never a failed behavioral oracle. For E2E-04 specifically, apply the procedure doc's usage-availability precondition before counting a missing gauge quote as FAIL (the gauge fails silent without token/context-window data). E2E-05 stays justified non-live (§4.6). The runtime under test is `WORKTREE`'s freshly built `cli/dist/cli.mjs` — never the committed checkout's build, never an installed `cline`. Results doc: `cline_context_bonsai/docs/e2e-results-<DATE>-live.md` + Run records row (side-repo commits per §4.6 Evidence paths).

### Phase 7: Release-gate steps 1–4 + seal gates (§2.9, §1.13)

18. §2.9 step 1 — tag the rebase point: from `WORKTREE`, `git tag bonsai/v1-on-cline-2.17.0` at the replayed tip.
19. §2.9 step 2 — e2e behavioral gate: Phase 6 evidence (Protocol A required always).
20. §2.9 step 3 — parent pin advance, local, on `pin-advance/cline-2.17.0`: the parent checkout's `cline/` submodule moves to the replay tip **by detached commit or tag checkout** (the replay branch is checked out in the worktree); commit the pin (and the side repo's pin if evidence commits landed).
21. §2.9 step 4 — pre-publish install gate: run the installation e2e in pre-publish mode per the runbook (bundle-sourced clone; bundle URL tails `context-bonsai-agents.git`, `cline.git`, `cline_context_bonsai.git`). PASS authorizes step 5 **as a separate owner-approved act**; FAIL → fix the README/install path and re-run; BLOCKED → resolve and re-run. Never proceed on FAIL/BLOCKED.
22. Seal gates 1–13 (§1.13), each machine-checkable: 12/12 rows mapped; zero hard-blocking rows; zero unapproved late fixes; replay-set checksum re-verified (`0d5692204c…`); no separate approval artifacts required (zero `manual_review` rows — §1.5's approvals file exists only to record such resolutions); baseline complete; provenance-trailer count 12 + final-state checks (`git merge-base 07593bb42a HEAD` = `07593bb42a`; `git rev-list --count 07593bb42a..HEAD` = 12; subjects match); post-replay ≥ baseline; change-scope union; no unresolved exceptions; e2e PASS evidence; spec-immutability assert; reviewer+judge approvals. Push-readiness dry-runs (§2.6) are deferred to the owner-approved publish continuation, with the pushes they precede.

### Phase 8: Cleanup — §1.19 scratch-state lifecycle (on BOTH seal and STOP paths)

The removal set is exactly the paths this run creates. Runs are serial; never remove another run's state.

- **On seal** (after the branch and tag are recorded in the fork repo — the refs, not the worktree, are durable): remove the worktree (`git -C $PARENT/cline worktree remove --force $PARENT/cline/.agent_tmp/rebase-on-v2.17.0-cli`), the shim symlink if it survives (`rm $PARENT/cline/.agent_tmp/cline_context_bonsai`), and the e2e scratch (`rm -rf /tmp/cline-bonsai-e2e`) after the results doc is committed. The replay branch stays (it is the tag's branch ref, rewritten next cycle per DEVELOPMENT.md chain discipline).
- **On STOP**: the run's scratch is run evidence — preserve until the STOP is dispositioned, then the disposition act removes it. Record the STOP reason code (§1.17) in the final and §1.16 maintenance reports.
- Parent-repo validation artifacts (plan, replay set, baseline, exceptions, rehearsal log) are durable cycle records, not cleanup targets.

## Validation Commands, grouped by working directory (§1.14 item 8)

### From `PARENT`
- `git status --short` (only enumerated dirty paths + this cycle's own artifacts)
- `jq -c < .agents/plans/validation/replay-set-f97ffd26a88685763d181393c282dc775b741530.json | sha256sum | cut -d' ' -f1` → `0d5692204c3c66ac3218977530b2dc25ba8b535ca59822925a851607ec08e2eb`
- `test -f docs/agent-specs/forward-port-spec.md && test -z "$(git diff --name-only -- docs/agent-specs/forward-port-spec.md)"`

### From `CLINE` (read-only inventory + frozen identities)
- `git fetch upstream "refs/tags/v2.17.0-cli:refs/tags/v2.17.0-cli"`; `git rev-parse --verify refs/tags/v2.17.0-cli^{commit}` → `07593bb42ae95793c6dbf39918c8205f0fdf07bb`
- `git rev-parse --verify refs/heads/feat/spec-compliance` → `f97ffd26a88685763d181393c282dc775b741530`
- `git merge-base 07593bb42ae95793c6dbf39918c8205f0fdf07bb f97ffd26a88685763d181393c282dc775b741530` → `70f0e8d548329f72e4c70378c377847d1f5ce2ed`
- `git log --topo-order --reverse --format='%H|%P|%s' 07593bb42a..f97ffd26a8` (12 rows); `git log --name-status --find-renames --format='%H|%s' 07593bb42a..f97ffd26a8`; `git cherry -v 07593bb42a f97ffd26a8` (12 `+`); `git log --merges 07593bb42a..f97ffd26a8` (empty)

### From `WORKTREE` root
- `git rev-parse HEAD` → `07593bb42a…` (before replay)
- `test -d node_modules || npm install`; `test -d cli/node_modules || (cd cli && npm install)`
- `npm run check-types` (r01 / post-replay)
- `npm run test:unit` (r03 / post-replay)
- `npm run cli:build`; `node cli/dist/cli.mjs version` (r04 / post-replay)
- `grep -c "context-bonsai-prune" cli/dist/cli.mjs` (post-replay, non-zero)
- `test "$(git log --format='%b' 07593bb42ae95793c6dbf39918c8205f0fdf07bb..HEAD | grep -cE '^\(cherry picked from commit [0-9a-f]{40}\)$')" = "12"`
- `git merge-base 07593bb42a HEAD` → `07593bb42a`; `git rev-list --count 07593bb42a..HEAD` → `12`; `git log --format='%s' 07593bb42a..HEAD` (subjects match)
- `git diff --name-status 07593bb42a..HEAD` (⊆ target_paths union); generated-artifact probe → `0`

### From `SIDE` (`cline_context_bonsai/`)
- `npm test` (r05, must-be-green); `npm run typecheck` (r06, must-be-green)

## E2E Gate section (§1.14 item 9)

Procedure: `cline_context_bonsai/docs/e2e-testing.md` (combined installation+runtime; binds all eight e2e-spec slots). Command binding: `docs/cline-e2e-runbook.md` — copy commands; flag gaps, never invent (§4.6 rule; no gap encountered at generation). Required minimum for the release-gate PASS (immutable, §4.6): live registration, E2E-01, E2E-02, E2E-03, E2E-06 (CLI half), E2E-07/Protocol A, plus the **first live E2E-04**. E2E-06's VS Code checkpoint half stays manual; E2E-05 stays justified non-live. Evidence recording: `cline_context_bonsai/docs/e2e-results-<DATE>-live.md` + Run records row; task-directory files and stdout captures stay in the rig.

## Worktree Artifact Check (§1.14 item 10)

- Checked at generation: 2026-07-06 (owner-tier generator).
- **Parent-repo generator writes**: this plan; the replay-set JSON; the two rehearsal logs (`rehearsal-cline-f97ffd26a.log`, `rehearsal-cline-siderepo.log`). Overlap: no pre-existing artifact keyed to this `SOURCE_HEAD_SHA` (`tracked-dirty`: none; `existing-untracked`: none) — no §1.14 collision. Other `.agents/plans/**` artifacts belong to other cycles' SHAs.
- **Executor writes**: baseline/exceptions JSONs — do not yet exist; the executor re-runs this check immediately before its first write.
- **Worktree targets**: created fresh in the new worktree; `TARGET_WORKTREE` and `TARGET_BRANCH` do not exist at generation (the rehearsal used the distinct `rehearsal-v2.17.0-cli` worktree, removed at loop close). The shim path `cline/.agent_tmp/cline_context_bonsai` persists from the rehearsal, content-identical to what Phase 2 creates — verify with `readlink` and reuse; not a collision.
- Escalation status: none. Decision citation: owner-tier generation under the Stage 6 acceptance gate (`derivation-pipeline-spec.md` §9).

## Plan Approval and Commit Status (§1.14 item 11)

- **Approval Status**: approved (routine local landing; execution of Phases 0–8 is in-scope routine work; §2.9 steps 5–6 pushes stay owner-gated).
- **Approval Citation**: standing owner order 2026-07-05 — local landings are routine and proceed autonomously; only outward actions wait. Reviewer+judge gates satisfied by the owner-tier generation + the §1.15 loop below.
- **Plan Commit Hash**: the commit introducing this file and the replay set (resolve with `git log --follow --oneline -1 -- .agents/plans/story-rebase-cycle-f97ffd26a88685763d181393c282dc775b741530.md`).
- **Ready-for-Orchestration**: YES — the §1.15 loop closed with zero blocking findings (below).

## Validation Loop Results (§1.15)

**Iteration 1 — target-resolution rehearsal (2026-07-06, owner tier): GREEN.** Disposable scratch worktree `cline/.agent_tmp/rehearsal-v2.17.0-cli` at `UPSTREAM_HEAD_SHA` (distinct from `TARGET_WORKTREE`), sibling shim `cline/.agent_tmp/cline_context_bonsai` → `../../cline_context_bonsai` created and verified. Full replay sequence applied: `git cherry-pick -x` of all 12 rows in topological order — **clean, zero conflicts**; provenance-trailer count 12/12. Hydration (`npm install` at root and `cli/`), then the Phase-5 validation rows: `npm run check-types` exit 0; `npm run test:unit` exit 0 (1447 passing); `npm run cli:build` exit 0; `node cli/dist/cli.mjs version` → `Cline CLI version: 2.17.0`; offline registration probe `grep -c "context-bonsai-prune" cli/dist/cli.mjs` = 25. Change-scope check: every path in `git diff --name-status 07593bb42a..HEAD` is inside the replay-set `target_paths` union. Side-repo rows from `cline_context_bonsai/`: `npm test` exit 0 (72 passing); `npm run typecheck` exit 0. Evidence logs: `.agents/plans/validation/rehearsal-cline-f97ffd26a.log`, `.agents/plans/validation/rehearsal-cline-siderepo.log`. Rehearsal worktree and its branch removed at loop close per §1.19; the shim symlink is retained deliberately for the execution phase (Worktree Artifact Check).

**Iteration 1 — missing-details pass** (independent, repository-inspecting): 1 blocking + 1 minor, both addressed. (a) The Validation Loop Results section was empty at the reviewer's snapshot (the rehearsal narrative landed moments later) → resolved by the recorded narrative above; the reviewer independently confirmed both rehearsal logs exist on disk and show the green results. (b) Minor: the fork's `check-types` script runs proto codegen and typechecks `src`/`webview-ui`/`cli`, broader than a bare tsc → clarifying note added to Phase 5. The reviewer independently re-verified all frozen SHAs, the merge-base, the 12/12 `+` cherry marks, zero merges, the replay-set checksum, the npm scripts, and every referenced doc/log — all matched.

**Iteration 1 — ambiguity pass** (independent, repository-inspecting): 3 blocking + 2 minor, all fixed. (a) The dirty-path enumeration's "and peers" catch-all was not executable, and the Stage 6 generation-time edits (spec, detector, runbook) were dirty in the working tree at review time → enumeration re-bound to the fixed submodule-pin pattern only, with the plan text now stating that the Stage 6 edits are committed before execution (plan-commit precondition), plus an always-record `sha256sum` instruction for gate 12's hash-reassertion form. (b) Gate 12 unsatisfiable while the spec sat dirty and unenumerated → same fix; at execution preflight the spec is clean (committed), and the recorded hash covers the enumerated-dirty contingency. (c) Phase 6 lacked the genuine-FAIL action → explicit STOP semantics added (`generation-plan-defect`, or `structural-protocol-a-regression` for a clean-build Protocol A failure), with Phase 0's leniency bounded to credential absence. Minor: E2E-04 usage-availability precondition restated in Phase 6; the second rehearsal log folded into the artifact-check and planned-targets enumerations. The reviewer verified the shim state, the scope boundary (Phases 0–8 vs §2.9 steps 5–6), and found both deterministic.

**Loop closed at iteration 1** (zero blocking findings remaining after fixes; rehearsal GREEN on the first pass; both reviewer passes recorded above). Loop-close cleanup per §1.19: rehearsal worktree `cline/.agent_tmp/rehearsal-v2.17.0-cli` and its branch removed; the shim symlink retained deliberately for the execution phase (Worktree Artifact Check).

## Completion Checklist (mirrors the seal gates)

- [ ] Every inventory item mapped to exactly one replay-set row (12/12)
- [ ] Zero unresolved `manual_review` / hard-blocking rows
- [ ] Zero unapproved late fixes; no source drift
- [ ] Replay-set present, schema-valid, checksum verified pre-replay and unchanged at seal
- [ ] Baseline artifact complete; must-be-green rows green; `r02` = missing-as-expected
- [ ] Replay verified by provenance trailers (12) + commit-range final-state checks
- [ ] Post-replay validation no worse than baseline; must-be-green rows green
- [ ] Change-scope union check passes; generated-artifact probe = 0
- [ ] No unresolved exception records
- [ ] E2E release gate PASS (required set incl. first live E2E-04); results doc committed
- [ ] §2.9 steps 1–4 complete (local tag, e2e, local pin advance, pre-publish install gate); steps 5–6 explicitly deferred to owner approval
- [ ] Spec immutability asserted
- [ ] Reviewer + judge approvals recorded
- [ ] §1.19 cleanup rows executed on the realized path (seal or STOP)

## §1.16 Maintenance duties (run after cycle end — sealed or STOP)

The executor performs routine maintenance and records the outcome (a STOP does not waive it):
- Absorb slot-level facts that changed into §4.6 with cited evidence; escalate core/shape (Parts 1–3) gaps to the owner tier. The routine executor edits Part 4 only; the edit is left uncommitted and its path named in the final report — the next cycle receives it as an enumerated dirty path until owner review lands.
- Give every stumble a `SPEC-GAP` vs `EXECUTOR-FAIL` verdict.
- If no slot-level fact changed, record that explicitly.
